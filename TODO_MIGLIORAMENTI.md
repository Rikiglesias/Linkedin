# TODO V2 — Miglioramenti Enterprise e Refactoring A 360°

Questo file contiene la checklist dei miglioramenti ancora sensati e realmente prioritari per portare la codebase a uno stato più robusto, coerente e manutenibile, evitando rewrite impulsive o task già coperti in modo sostanziale.

---

## 1. Frontend, UX Operativa e Surface Prodotto (Priorità: ALTA - CRITICO)
Il frontend attuale ha già molte capacità operative reali, ma va reso più modulare, coerente e difendibile nel tempo.

- [ ] **Spezzare `src/frontend/main.ts`** (in corso). Estratti: `realtime.ts` (115 righe: SSE/WebSocket/reconnect), `leadSearch.ts` (97 righe: bindLeadSearch + bindBlacklist). Voice Commands rimosso (240+ righe). main.ts da 1063 a 559 righe (-47%). Restano da estrarre: `controls.ts` (pause/resume/trigger), `leadSearch.ts`, `exports.ts`, `voice.ts`. Il file contiene SSE/WebSocket connection management, polling loop, voice recognition, UI event bindings, rendering orchestration e localStorage prefs — tutto nello stesso file. Estrazione consigliata: `realtime.ts` (SSE + WebSocket + reconnect logic, ~200 righe), `controls.ts` (pause/resume/run/quarantine handlers, ~100 righe), `leadSearch.ts` (search form + detail modal, ~150 righe), `exports.ts` (CSV/PNG/print, ~50 righe), `voice.ts` (SpeechRecognition setup + command dispatch, ~120 righe), `snapshotLoader.ts` (loadSnapshot + polling orchestration, ~200 righe). File coinvolti: `main.ts`, `renderers.ts` (676 righe, 27KB), `apiClient.ts` (233 righe). Il file `renderers.ts` è già ben separato, ma `main.ts` deve diventare solo un bootstrapper che importa i moduli.
- [x] **Definire un design system minimo condiviso** per token visivi, pattern accessibili, stati UI e dark mode coerente. Attualmente `public/style.css` (20KB) usa valori hardcoded per colori, spacing e font. Convertire in CSS custom properties (`--color-primary`, `--color-danger`, `--spacing-sm`, ecc.) in un blocco `:root` + `[data-theme="dark"]`. Il tema dark è già implementato via classe CSS ma con duplicazione di valori tra light e dark. Centralizzare in un `design-tokens.css` importato da `style.css`.
- [x] **Riallineare asset frontend e policy di sicurezza**. Il `server.ts` (riga 181) setta `Content-Security-Policy: script-src 'self'`, ma `public/index.html` (riga 16) carica `<script src="https://cdn.jsdelivr.net/npm/chart.js@4/dist/chart.umd.min.js">` da CDN esterno — violazione diretta della CSP. O bundlare Chart.js localmente (è già in `dependencies` del `package.json`) nel build esbuild, oppure aggiungere `cdn.jsdelivr.net` alla CSP con SRI hash. La prima opzione è preferibile: esbuild lo gestisce nativamente, il bundle attuale (`public/assets/bundle.js`) ha Chart.js come `external` — rimuovere l'external e includere nel bundle.
- [ ] **Introdurre un data layer più robusto** per fetch/cache/error handling/loading state. `src/frontend/apiClient.ts` ha già un `DashboardApi` con cache TTL per endpoint (righe 34-45), ma: (a) gli errori API ritornano `fallback` silenziosamente senza feedback UI (riga 101-103); (b) non c'è loading state (l'utente vede "0" mentre i dati caricano); (c) `readJson()` non distingue tra errore rete, 401 auth, e 500 server. Aggiungere: enum `FetchState` (`idle|loading|success|error`), gestione 401 con redirect a login, retry automatico per errori transitori (429, 503), e skeleton/shimmer rendering durante loading.
- [ ] **Migliorare la UX della control room** su incident triage, feedback azioni critiche, stati degraded/disconnected e leggibilità su viewport piccoli. Specificamente: (a) il bottone "Risolvi" sugli incidenti non ha conferma — un click errato risolve senza undo; (b) lo stato SSE disconnesso mostra solo "Connessione..." senza indicare da quanto tempo è offline; (c) `public/style.css` ha alcune `@media` query ma non è testato sotto 768px — la KPI grid e la tabella trend non hanno overflow-x scroll su mobile; (d) la sezione lead-search non ha debounce sull'input (ogni keypress fa una fetch).
- [ ] **Valutare una migrazione progressiva a React/Vite** solo dopo la modularizzazione del frontend attuale. I file frontend attuali sono: `main.ts` (1057 righe), `renderers.ts` (676), `apiClient.ts` (233), `charts.ts` (145), `dom.ts` (90), `i18n.ts` (261), `timeline.ts` (85), `types.ts` (130), `voiceCommands.ts` (116) — totale ~2800 righe. Il costo di migrazione stimato è 2-3 settimane. Benefici reali: component isolation, state management, HMR, test con React Testing Library. Prerequisito: completare prima i 4 task di modularizzazione sopra.
- [x] **Chart.js da CDN senza SRI**: `public/index.html` riga 16 carica `chart.js@4` da `cdn.jsdelivr.net` senza attributo `integrity` né `crossorigin`. Fix preferito: rimuovere il tag `<script>` esterno e includere Chart.js nel bundle esbuild (rimuovere `external: ['chart.js','chart.js/auto']` da `package.json` riga 9 nel build:frontend). Fix alternativo: aggiungere `integrity="sha384-..."` + `crossorigin="anonymous"` e aggiungere `cdn.jsdelivr.net` alla CSP in `server.ts` riga 181.
- [ ] **Service Worker stratificato**: `public/sw.js` (2159 bytes) è uno shell minimo con solo `install`/`activate` base. Non fa caching strategico. Implementare: cache-first per `/assets/*` (bundle JS, CSS) con versioning, network-first per `/api/*` con fallback offline, precaching del bundle compilato durante `install`. Usare Workbox (via `workbox-webpack-plugin` o generazione manuale del manifest).
- [x] **Bundle frontend non minificato**: il build esbuild in `package.json` riga 9 non specifica `minify: true`. Aggiungere `minify:true` all'oggetto `buildSync()`. Quick win: riduce il bundle size del 40-60% senza effetti collaterali. Aggiungere anche `drop:['console']` per produzione (opzionale).
- [x] **Toast notification system**: implementato container `#toast-stack` in `index.html`, CSS completo in `style.css` (4 severity con border-left colorato, animazione in/out, max 5 toast, auto-dismiss). Funzione `showToast(message, severity, durationMs)` in `dom.ts` con icone SVG inline, escaping HTML. Integrato in `main.ts` per: pause, resume, trigger-run, resolve incident, export CSV/PNG, errori API/rete.

## 2. Architettura Backend, Runtime Boundaries e Scalabilità (Priorità: ALTA)
Il punto critico non è “fare microservizi” in astratto, ma separare bene responsabilità, lifecycle e failure domain.

- [ ] **Separare il processo API/Dashboard dal processo Worker/Browser** con entrypoint, healthcheck e lifecycle indipendenti. Attualmente `src/index.ts` (616 righe) gestisce sia il comando `dashboard` (che avvia Express su porta 3000) sia tutti i comandi operativi (`run`, `autopilot`, `run-loop`) nello stesso processo. Il problema: un crash del browser Playwright abbatte anche l'API dashboard, e viceversa un memory leak dell'API blocca i job. Approccio: creare `src/entrypoints/api.ts` e `src/entrypoints/worker.ts`, ciascuno con il proprio graceful shutdown. In `docker-compose.yml` diventano due servizi (`bot-api` e `bot-worker`) con healthcheck indipendenti. Il DB (SQLite o PG) è già condiviso via file/connessione — non serve IPC. La coda job (`jobs` table) funge da broker naturale.
- [x] **Formalizzare contratti worker con registry**. Creato `workers/registry.ts` con interfaccia `WorkerProcessor` e `workerRegistry: Map<JobType, WorkerProcessor>`. 7 worker registrati (invite, acceptance, message, hygiene, interaction, enrichment, post_creation). Il switch/case da ~40 righe nel jobRunner sostituito con 7 righe di lookup. `parseJobPayload` ancora usato per i path secondari (campaign advance, challenge extraction, dead letter). Stato in-memory (activeBrowsers, proxyFailureUntil, circuitStates) resta come debito tecnico noto.
- [ ] **Valutare un broker persistente (Redis + BullMQ)** solo come step successivo alla separazione API/worker. Attualmente la coda è una tabella `jobs` in SQLite/PG con colonne `status`, `scheduled_at`, `attempts`, `locked_at`. Il pattern `lockNextQueuedJob()` in `repositories/jobs.ts` usa `UPDATE ... WHERE status='QUEUED' LIMIT 1` come lock ottimistico. Questo funziona per 1 worker ma non scala a N worker paralleli senza race condition (SQLite non ha `FOR UPDATE SKIP LOCKED`; PG sì). Redis + BullMQ risolverebbe: job deduplication nativa, delayed jobs, retry con backoff, dashboard Bull Board. Prerequisito: separazione processi completata (task precedente).
- [ ] **Aggiornare `docker-compose.yml`** per supportare scaling separato. File attuale: `docker-compose.yml` (91 righe) con 3 servizi (db, bot, dashboard-nginx). Modifiche: (a) separare `bot` in `bot-api` e `bot-worker`; (b) il worker non espone porte, l'API espone 3000; (c) nginx fa proxy solo verso `bot-api`; (d) aggiungere `restart: unless-stopped` + `deploy.resources.limits.memory: 2G` per il worker (Playwright è memory-hungry); (e) aggiungere volume named per `data/session*` condiviso tra API e worker se necessario.
- [ ] **Ridurre config drift con profili ambiente**. `src/config/domains.ts` (418 righe) costruisce la config leggendo ~200 variabili `.env` senza grouping logico. `src/config/validation.ts` (398 righe) ha ~50 regole di validazione ma solo sui valori individuali, non sulle combinazioni cross-dominio. Miglioramenti: (a) introdurre profili `CONFIG_PROFILE=dev|staging|production` con override di default per ambiente; (b) aggiungere validazioni cross-dominio (es. `COMPLIANCE_ENFORCED=true` dovrebbe forzare `HARD_INVITE_CAP <= COMPLIANCE_MAX_HARD_INVITE_CAP`); (c) raggruppare le ~200 variabili in sezioni con prefix coerenti nel `.env.example` (già fatto parzialmente, ma mancano i gruppi per proxy, timing, growth model).
- [x] **Splittare `src/api/server.ts`** — COMPLETATO. Estratti 7 moduli: `routes/stats.ts` (9 endpoint), `routes/ai.ts` (5 endpoint), `routes/security.ts` (4 endpoint), `routes/blacklist.ts` (3), `routes/leads.ts` (2), `routes/controls.ts` (4), `helpers/controlActions.ts` + `helpers/requestIp.ts` + `helpers/audit.ts`. server.ts da 1722 a 973 righe (-43%). Restano in server.ts: auth middleware, SSE/WebSocket, health endpoints, v1 API, rate limiting, CORS, CSP — logica core del server che non ha senso estrarre.
- [ ] **Splittare `src/salesnav/bulkSaveOrchestrator.ts` (112KB)**: file più grande della codebase. Decomporre in: `pageNavigator.ts`, `leadExtractor.ts`, `saveProcessor.ts`, `retryHandler.ts`, `orchestrator.ts`.
- [ ] **Migrazione ESM**: `package.json` usa `"type": "commonjs"` con target TS `ES2022`. Frizione con librerie ESM-only e futuro Node. Pianificare migrazione a `"type": "module"` con aggiornamento import paths.
- [ ] **Repository pattern incompleto**: i repository in `src/core/repositories/` mescolano query SQL raw con business logic. Separare nettamente data access (SQL/CRUD) dai service che contengono logica.
- [x] **Error handling utility uniforme**: creata `safeAsync(fn, fallback?, label?)` in `src/utils/async.ts`. Cattura errori e logga a `console.warn` con label. Disponibile per uso incrementale nei nuovi moduli (97 occorrenze legacy in 27 file — migrazione progressiva, non big-bang).
- [ ] **Dependency Injection leggera**: il codice usa import diretti ovunque, rendendo difficile mockare e testare. Introdurre un `AppContext` passato esplicitamente con database, config, logger, proxyManager.
- [x] **SQL normalization cache LRU**: `PostgresManager.normalizeSql()` esegue 8+ regex replace ad ogni query. Le stesse SQL string vengono ripetute migliaia di volte. Cacheare il risultato con LRU map.

## 3. Anti-Ban, Browser Reliability e Self-Healing (Priorità: MEDIA)
La direzione corretta è aumentare coerenza e verificabilità, non spingere su tecniche sempre più evasive e fragili.

- [ ] **Rafforzare self-healing DOM-first**. `src/browser/uiFallback.ts` (505 righe) ha già un sistema sofisticato: ranking candidati con `scoreSelectorCandidate()` (stabilità, confidenza, success history), context cache LRU (300 entry, 6h TTL), dynamic selectors dal DB. Aree da migliorare: (a) **Shadow DOM coverage** — `page.locator()` non penetra Shadow DOM chiusi; LinkedIn usa Web Components in alcune sezioni (messaging, notifiche). Aggiungere `page.locator('pierce/...')` o `page.evaluate()` con `querySelector` ricorsivo su `shadowRoot`; (b) **verifica post-azione** — dopo ogni click/type, verificare che l'azione abbia avuto effetto (es. dopo click "Connect", verificare che il modale nota sia apparso); (c) `src/selectors/learner.ts` (13KB) gestisce il ranking persistente — aggiungere metriche di "selector drift" per rilevare quando LinkedIn cambia i class name (es. `artdeco-button` → nuovo prefisso).
- [x] **Vision fallback tracciato**. Aggiunto `visionFallbackUsed?: boolean` a `WorkerExecutionResult` in `result.ts`. In `hygieneWorker.ts`, variabile `visionUsed` diventa `true` quando almeno uno dei 3 vision fallback (pending, withdraw dropdown, confirm modal) viene usato. Il flag viene incluso nel log `job.worker_result` del jobRunner per monitorare se il vision sta mascherando selettori rotti.
- [x] **Misurare incoerenze fingerprint/stealth**. Creato `src/tests/fingerprint-coherence.vitest.ts` con 14 test: per ogni fingerprint desktop verifica UA contiene OS desktop + viewport >=1024px + hwConcurrency [4-16] + deviceMemory [4-16] + colorDepth 24. Per ogni fingerprint mobile verifica UA contiene mobile indicator + viewport <500px + hasTouch + deviceScaleFactor >=2 + hwConcurrency [4-8] + deviceMemory [2-6] + colorDepth 32. Test cross-pool: ID univoci, noise deterministico e in range [0, 0.01]. La logica `deriveHw` replica esattamente `launcher.ts` righe 399-411 per verificare che i valori assegnati siano coerenti con il tipo device.
- [x] **Regressioni testabili anti-detection**. Creato `src/tests/stealth.vitest.ts` con 16 test: JS parsabile, lunghezza >5KB, 12 check presenza mock (webdriver, plugins, chrome.runtime, WebRTC, Notification, Battery, AudioContext, CDP, hwConcurrency, deviceMemory, language, iframe chrome), headless guard extra, skipSections rispettate. Test con browser headless reale (Playwright in test mode) è un task futuro.
- [x] **Criteri rollback/disable per stealth sperimentali**: implementato `STEALTH_SKIP_SECTIONS` in `.env` (CSV). Parsato in `config/domains.ts`, merge con CloakBrowser skip in `launcher.ts`. Il meccanismo `_skip` Set in `stealthScripts.ts` riceve tutte le sezioni da entrambe le sorgenti. Valori possibili: webrtc, plugins, hwconcurrency, battery, audio, canvas, webgl.
- [x] **Fingerprint deterministico per account**: `pickBrowserFingerprint()` in `stealth.ts` ora usa FNV-1a hash con `accountId:cloud:weekN` come seed per il cloud pool (prima era `randomElement`). `pickFingerprintMode()` ora accetta `accountId` e decide mobile/desktop deterministicamente per account+settimana (prima era `Math.random()`). Il path locale usava già `pickDeterministicFingerprint()` con FNV-1a. Ora TUTTI i path sono deterministici.
- [x] **Canvas noise con PRNG Mulberry32**: sostituito il pattern fisso `pixelIndex % 2/3/5` in `launcher.ts` con PRNG Mulberry32 seedato da `canvasNoise * 1e9` (derivato dal fingerprint). I segni R/G/B sono ora pseudo-casuali ma deterministici per sessione — non rilevabili statisticamente. Periodo 2^32, veloce, zero dipendenze.
- [x] **WebGL renderer pool espanso**: da 2 a 12 valori realistici in `launcher.ts`. Pool desktop: 3 Intel (Iris Xe, UHD 630, UHD 770), 3 NVIDIA (GTX 1650, RTX 3060, RTX 4070), 2 AMD (RX 580, Radeon Graphics). Pool Apple: 4 (GPU, M1, M2, M3). Selezione deterministica per fingerprint via `canvasNoise * 1e6 % pool.length`.
- [x] **humanWindDown prevedibile**: alla chiusura il bot naviga **sempre** verso `/feed`. Pattern costante. Fix: variare destinazione (feed, notifiche, homepage, /mynetwork, oppure restare sulla pagina corrente) con distribuzione pesata.
- [x] **JA3 fingerprint pool — NON APPLICABILE**. CycleTLS è stato rimosso completamente (Sez.15). Senza CycleTLS, Playwright usa lo stack TLS nativo di Chromium. Il proxy Oxylabs mobile fornisce TLS handshake nativo. Nessun JA3 spoofing necessario.
- [x] **Typing speed variabile per contesto**: implementato `lengthSlowFactor` in `humanBehavior.ts` (vedi sez.9 humanType context-aware). Il delay base viene modulato per lunghezza testo: ≤30 char ×0.85, 30-150 ×1.0, 150-400 ×1.15, >400 ×1.3.
- [x] **Session cookie anomaly detection**: implementato `detectSessionCookieAnomaly()` in `sessionCookieMonitor.ts`. Traccia hash SHA-256 del cookie `li_at` nel `.session-meta.json`. Rileva: `COOKIE_MISSING` (scomparso → quarantine + alert Telegram critical) e `COOKIE_CHANGED` (cambiato senza rotazione → alert Telegram warn). Integrato in `jobRunner.ts` dopo `recordSuccessfulAuth()` e prima della probe LinkedIn.

## 4. Sicurezza, Governance e Threat Model Reale (Priorità: ALTA)
Serve blindare meglio le superfici davvero esposte e riallineare ciò che è dichiarato in documentazione con ciò che succede davvero nel codice.

- [x] **Allineare documentazione e implementazione reale**. Corretto in `SECURITY.md`: (a) righe 3-5 riscritta sezione "What is already hardened" — ora documenta i 18 stealth patches, STEALTH_SKIP_SECTIONS, fingerprint deterministico, PRNG Mulberry32; (b) sezione AI aggiornata: documenta esplicitamente `AI_ALLOW_REMOTE_ENDPOINT=true` come scelta intenzionale con guardrail; (c) aggiunto: dashboard session TTL 12h, brute-force lockout 5/15min→30min, export endpoints rate-limited 5/h; (d) CSP ora corretto (Chart.js bundlato).
- [x] **Auth bootstrap via query string documentato**. Aggiunto warning in `SECURITY.md` sezione "Dashboard auth bootstrap warning": documenta il rischio URL query string, raccomanda uso solo su localhost/trusted network, e indica che form-based login è pianificato. Il fix completo (form POST login) è un task frontend separato.
- [ ] **Integrare un Secret Manager Esterno** per produzione. Attualmente TUTTI i segreti (OpenAI key, Supabase service role key, proxy password, Telegram bot token, dashboard API key, webhook secret, Apollo API key) sono nel `.env` da 18KB in chiaro. Per produzione: integrare con **Doppler**, **Infisical** (self-hosted, gratuito), o **1Password CLI** (`op inject`). L'integrazione minima è un wrapper in `config/env.ts` che prova prima il secret manager e poi fallback su `.env`. In alternativa: Docker Secrets (`/run/secrets/`) per i deployment containerizzati.
- [ ] **Implementare 2FA/TOTP per dashboard**. L'auth attuale (`server.ts` righe 462-577) supporta: API key, Basic auth, session cookie, trusted IP. Nessun secondo fattore. Per aggiungere TOTP: (a) generare un TOTP secret per utente (libreria `otpauth` o `speakeasy`); (b) aggiungere un endpoint `POST /api/auth/totp/verify` che valida il codice 6 cifre; (c) il session cookie viene emesso solo dopo API key/Basic + TOTP valido; (d) salvare il TOTP secret cifrato nel DB o in `.env` (`DASHBOARD_TOTP_SECRET`). Alternativa più semplice per single-user: TOTP via Telegram bot (manda il codice su Telegram, l'utente lo inserisce).
- [x] **Risk engine spiegabile: endpoint `/api/risk/explain`**. Implementato `explainRisk()` in `riskEngine.ts` + endpoint `GET /api/risk/explain` in `server.ts`. Mostra: (a) contributo di ogni fattore allo score (errorRate×40, selectorFailureRate×20, pendingRatio×25, challengeCount×10, inviteVelocityRatio×15); (b) trigger attivi che hanno causato l'azione (es. "challengeCount=1 > 0 → STOP"); (c) soglie configurate (riskWarn, riskStop, pendingRatioWarn, pendingRatioStop). L'utente vede esattamente PERCHÉ il risk engine blocca o rallenta. Weight tuning e log strutturato restano come task futuri.
- [x] **Checklist periodica SECURITY.md / THREAT_MODEL.md — GIÀ IMPLEMENTATA (parte a)**. `securityAdvisor.ts` righe 333-395: check age `THREAT_MODEL.md` e `SECURITY.md` vs `SECURITY_ADVISOR_DOC_MAX_AGE_DAYS`. Status OK/WARN/FAILED. Inoltre: secret rotation check, open incidents count, DR restore drill. La parte (b) — verifica coerenza config runtime vs documentazione — richiede parsing strutturato dei doc ed è un task futuro di bassa priorità.
- [x] **Docker container gira come root**: il `Dockerfile` non ha `USER` directive. Aggiungere `RUN groupadd -r botuser && useradd -r -g botuser botuser` + `USER botuser` prima del `CMD`. Riduce drasticamente la superficie d'attacco in caso di RCE.
- [x] **nginx senza HTTPS/TLS documentato**: aggiunta nota in cima a `nginx.conf` che spiega la scelta HTTP-only e la delegazione TLS al reverse proxy esterno (Cloudflare, Caddy, Traefik). Se esposto direttamente a Internet → aggiungere TLS con Let's Encrypt.
- [x] **PG backup implementato**: `backupDatabase()` per PostgreSQL ora esegue `pg_dump` via `execFileSync` con `--dbname` connection string, `--format plain`, `--no-owner`, `--no-privileges`. Output in `data/backups/pg_backup_YYYY-MM-DDTHH-MM-SS.sql`. Timeout 120s. Se `pg_dump` non è installato → warning + stringa informativa (non crash). Sicurezza: `execFileSync` (no shell injection). Il task `auto_backup` in `loopCommand.ts` ha `onError: 'skip'` → il loop non si blocca.
- [x] **VACUUM INTO con interpolazione SQL fixato**: sostituito `VACUUM INTO '${safePath}'` in `db.ts` con `PRAGMA wal_checkpoint(TRUNCATE)` + `fs.copyFileSync()`. Nessun path nella SQL, nessun rischio injection. WAL mode confermato attivo (riga 417).
- [x] **Rate limit per session/API key**: implementato `rateLimitKeyGenerator` in `server.ts`. Identifica il client per: API key (primi 8 char), session cookie (primi 8 char), o IP come fallback. Applicato a `globalLimiter` e `controlsLimiter`. Dietro NAT ogni client autenticato ha il proprio rate limit indipendente.

## 5. Database, Persistenza e Strategia Data Layer (Priorità: MEDIA)
Le decisioni qui vanno prese su profili di carico reali, non per moda architetturale.

- [ ] **Profilare query hot-path e lock contention**. I repository in `src/core/repositories/` (14 file, ~230KB totali) contengono centinaia di query SQL raw. Le hot-path sono: `lockNextQueuedJob()` in `jobs.ts` (chiamata ad ogni job cycle), `getDailyStat()` in `stats.ts` (chiamata ~10 volte per ciclo orchestrator), `getRiskInputs()` in `stats.ts` (3 subquery aggregate). In SQLite, `lockNextQueuedJob` usa `UPDATE ... WHERE id = (SELECT id ... LIMIT 1)` che non ha row-level lock — sotto carico concorrente può causare `SQLITE_BUSY`. Il `busy_timeout` è 5000ms (`db.ts` riga 406). Approccio: aggiungere `EXPLAIN QUERY PLAN` logging per le top-20 query in un mode diagnostico, misurare tempo medio e P99, identificare index mancanti.
- [ ] **Layer type-safe incrementale** per le nuove aree complesse. Il candidato migliore è **Kysely** (zero runtime overhead, query builder type-safe, supporta SQLite + PG dialetti). Drizzle è un'alternativa ma ha un ORM layer che aggiunge complessità. Strategia: (a) introdurre Kysely solo per i nuovi moduli (es. `repositories/campaigns.ts`, `repositories/featureStore.ts` che sono i più recenti); (b) generare i tipi DB con `kysely-codegen` dal schema esistente; (c) mantenere i repository legacy con SQL raw fino a quando non vengono toccati per altre ragioni. Non fare mai una migrazione big-bang — il costo di riscrivere le ~500 query esistenti non giustifica il beneficio.
- [ ] **Partitioning PostgreSQL**. Le tabelle candidate sono: `lead_events` (cresce linearmente con ogni transizione lead), `job_attempts` (ogni tentativo di job), `outbox_events` (ogni evento sincronizzato), `run_logs` (ogni ciclo). In `src/db/migrations/` ci sono 50 migrazioni ma nessuna definisce partitioning. In PG16: usare `PARTITION BY RANGE (created_at)` con partizioni mensili. Prerequisito: essere in produzione su PG (attualmente SQLite in dev). Non implementare su SQLite — non supporta partitioning.
- [x] **Strategia retention coerente**. PM2 `max_size: "50M"` per log rotation. `RETENTION_DAYS=90`, `BACKUP_RETENTION_DAYS=7` configurati. Privacy cleanup automatizzato nel `run-loop` come sub-task ogni 24h (sez.16). Resta solo: gestire backup manuali in `data/` (file locked, richiede intervento manuale).
- [ ] **Verificare coerenza SQLite vs PostgreSQL**. `src/db.ts` (456 righe) ha un adapter pattern con `SQLiteManager` e `PostgresManager`. Il `normalizeSql()` di PG (righe 89-134) converte: `?` → `$N`, `DATETIME('now')` → `CURRENT_TIMESTAMP`, `INSERT OR IGNORE` → `ON CONFLICT DO NOTHING`, `strftime` → `TO_CHAR`. Ma: (a) `VACUUM INTO` (riga 451) è SQLite-only — per PG non fa nulla; (b) le migrazioni in `src/db/migrations/` sono scritte in dialetto SQLite e convertite on-the-fly per PG (righe 264-270) — conversioni regex fragili; (c) `json_set()` usata in `postCreatorWorker.ts` (riga 73) è SQLite-specific, PG usa `jsonb_set()`. Creare un test che esegue tutte le migrazioni e le query principali su entrambi i dialetti per catturare divergenze.

## 6. Workflow, Test, CI/CD e Qualità Ingegneristica (Priorità: ALTA)
Il problema non è solo “avere test”, ma avere test leggibili, granulari e allineati ai rischi veri del sistema.

- [ ] **Spezzare `src/tests/integration.ts` (78KB) e `src/tests/unit.vitest.ts` (54KB)**. Il file integration è uno script `ts-node` standalone che non usa vitest — non beneficia di reporters, watch mode, parallel execution. Il file unit contiene TUTTI i test vitest in un singolo file. Splitting per unit: `risk.vitest.ts`, `config.vitest.ts`, `scheduler.vitest.ts`, `db.vitest.ts`, `selectors.vitest.ts`, `redaction.vitest.ts`. Per integration: migrare a vitest con `describe`/`it`, usare `beforeAll`/`afterAll` per setup DB SQLite in-memory, separare in `integration/db.vitest.ts`, `integration/repositories.vitest.ts`, `integration/sync.vitest.ts`.
- [ ] **Test mirati per contratti sicurezza e runtime**. Moduli senza test dedicati: (a) `server.ts` auth middleware — testare 401 senza credenziali, trusted IP bypass, session cookie, brute-force lockout dopo 5 tentativi; (b) CSRF — POST da origin non-localhost ritorna 403; (c) CSP header corretto; (d) SSE/WebSocket degraded path; (e) `leadStateService.ts` — transizioni non consentite rifiutate (mappa `allowedTransitions` righe 9-23). Usare `supertest` per i test HTTP.
- [ ] **E2E su ambienti controllati**. Il CI (`ci.yml` righe 97-99) ha `test:e2e:dry` ma è un dry-run senza browser. Per E2E reali: usare Playwright Test con mock server Express che simula risposte LinkedIn (JSON fixtures). Testare workflow critici senza toccare LinkedIn: `login → doctor → schedule → jobRunner mock → report`. Per la dashboard: avviare Express in test mode, navigare con Playwright, verificare KPI, pause/resume, SSE.
- [ ] **Strategia release/deploy**. Nessun versioning semantico (`package.json` version fisso `1.0.0`), nessun changelog, nessun tag git. Il CI fa build Docker ma senza push a registry. Fix: (a) adottare `standard-version` o `changesets`; (b) push immagine a GitHub Container Registry con tag `latest` + `sha`; (c) documentare rollback: restore backup SQLite, `git revert`, re-deploy container precedente; (d) estendere smoke test CI (righe 113-137) con check `/api/kpis` oltre a `/api/health`.
- [x] **Coverage threshold in CI**: il CI esegue `vitest run` senza `--coverage` e senza soglia minima. Aggiungere `vitest run --coverage` con threshold (es. 60% come baseline, poi incrementare). Impedisce regressioni silenziose di copertura.
- [ ] **E2E Playwright per la dashboard**: il progetto USA Playwright per l'automazione LinkedIn ma non per testare la propria UI. Aggiungere test E2E: login dashboard, KPI visibili, pause/resume flow, SSE connection, lead search. Copertura reale della superficie operativa.
- [ ] **Mutation testing**: per un bot dove un singolo bug = ban LinkedIn, il mutation testing (es. Stryker) verifica che i test catturano realmente le mutazioni nei moduli critici (riskEngine, scheduler, cooldown, compliance). Da applicare almeno ai moduli safety-critical.
- [x] **Automazione dipendenze**: `.github/dependabot.yml` già configurato (weekly npm minor+patch, monthly Actions, grouping). CodeQL SAST nel CI. Nessun tool aggiuntivo necessario.

## 7. AI, Provider Strategy e Personalizzazione Dati (Priorità: BASSA)
Qui serve un’evoluzione governata, non più “AI” sparsa o fallback poco coerenti tra loro.

- [ ] **Unificare il contratto tra provider AI/vision/local-first**. Attualmente ci sono 3 client separati: `src/ai/openaiClient.ts` (5.6KB, gestisce OpenAI + Ollama via endpoint compatibile), `src/captcha/visionProviderFactory.ts` (8.9KB, crea VisionProvider con cascata auto/openai/ollama), `src/captcha/ollamaVisionProvider.ts` (1.7KB, client Ollama diretto). Il problema: la logica di fallback (GPT-5.4 → Ollama) è implementata separatamente nel vision factory e non è riusabile dall'AI client. Creare un `src/ai/providerRegistry.ts` che centralizza: endpoint resolution, API key validation, budget tracking, fallback chain. I moduli consumer (`messagePersonalizer`, `inviteNotePersonalizer`, `sentimentAnalysis`, `challengeHandler`, `computerUse`) importano dal registry invece dei client specifici.
- [x] **Quality gates per flussi AI sensibili**. Challenge handler: cap 3/giorno con reset a mezzanotte (bug L4 fixato). INBOX_AUTO_REPLY: 5 gate attivi — `inboxAutoReplyEnabled` (default false), `!context.dryRun`, `autoRepliesSent < maxPerRun` (2), `confidence >= minConfidence` (0.9), `replyDuplicateCount === 0`, esclusione intent NOT_INTERESTED/NEGATIVE. AI_GUARDIAN: logging passivo coperto da `logInfo('inbox.intent_resolved')` con confidence, intent, subIntent, reasoning.
- [ ] **Valutare RAG con limiti chiari**. Un modulo RAG (Retrieval-Augmented Generation) potrebbe migliorare la personalizzazione messaggi usando i dati scrappati dal profilo lead (about, experience, posts). Prerequisiti prima di implementare: (a) verificare che `PROFILE_CONTEXT_EXTRACTION_ENABLED` (attualmente `false`) funzioni correttamente quando abilitato — `src/browser/linkedinProfileScraper.ts` (8.8KB) estrae about/experience; (b) definire retention: i dati profilo vanno cancellati dopo `RETENTION_DAYS`; (c) costi: con Ollama locale il costo è zero ma la qualità è inferiore; con OpenAI ogni messaggio personalizzato costa ~$0.002-0.01; (d) privacy: non inviare dati PII a endpoint cloud se `AI_ALLOW_REMOTE_ENDPOINT=false`; (e) osservabilità: loggare quale context è stato usato per ogni messaggio generato. Non implementare prima di avere i quality gates del task precedente.
- [ ] **Mantenere principio local-first**. La config attuale nel `.env` reale contraddice questo principio: `AI_ALLOW_REMOTE_ENDPOINT=true` + `OPENAI_BASE_URL=https://api.openai.com/v1`. Questo è intenzionale per qualità superiore (GPT-5.4 vs Ollama llama3.1:8b), ma va documentato esplicitamente. Approccio: (a) aggiornare `SECURITY.md` per riflettere la scelta cloud-first attuale; (b) mantenere il flag `AI_ALLOW_REMOTE_ENDPOINT` come guardrail — se impostato a `false`, il bot usa solo Ollama locale; (c) aggiungere un fallback automatico: se OpenAI è down/429, scalare a Ollama locale senza interrompere il flusso (attualmente `openaiClient.ts` fa già fallback a template, ma non a Ollama); (d) per il modello green mode (`AI_GREEN_MODEL=llama3.1:8b`), usare sempre Ollama indipendentemente da `AI_ALLOW_REMOTE_ENDPOINT`.

## 8. DevOps, Pulizia Codebase e Observability (Priorità: MEDIA)
Sezione completamente nuova emersa dall'analisi 360°. Copre infrastruttura, igiene del repository e monitoring — aspetti non presenti nelle sezioni precedenti.

### File root da rimuovere o spostare
- [x] **`lint.json`, `lint_output.txt`, `typecheck_output.txt`**: già aggiunti al `.gitignore` (righe 35-37).
- [x] **`.env.bak` (4.7KB)** — verificato: NON tracked in git (`git ls-files .env.bak` = vuoto). Il `.gitignore` copre già il pattern. Nessuna azione necessaria.
- [x] **`repomix.config.json`** — aggiornato: `INTEGRATIONS.md` → `docs/INTEGRATIONS.md`. Include già `TODO_MIGLIORAMENTI.md` e `SECURITY.md`. `ROADMAP.md` non presente (già rimosso in sessione precedente).
- [x] **`.claude/` directory**: già aggiunto `.claude/` al `.gitignore` (riga 43).

### Documentazione root — consolidamento
- [x] **Troppi `.md` in root — consolidati in `docs/`**. Spostati con `git mv`: `AI_QUALITY_PIPELINE.md` → `docs/`, `CONFIG_REFERENCE.md` → `docs/`, `INTEGRATIONS.md` → `docs/`. Aggiornati tutti i riferimenti: `README.md` (3 link + tree struttura), `repomix.config.json` (path INTEGRATIONS), `scripts/generate-config-docs.mjs` (output path), `.dockerignore` (aggiunta `docs/` + `README.md`). In root restano solo: `README.md`, `SECURITY.md`, `THREAT_MODEL.md`, `GUIDA_ANTI_BAN.md`, `TODO_MIGLIORAMENTI.md`.
- [x] **`AUDIT_COMPLETAMENTI_2026.md` (39KB)** — spostato in `docs/archive/AUDIT_COMPLETAMENTI_2026.md` con `git mv`. File storico, non operativo.
- [x] **`TODO.md` (75KB)** — verificato: 0 punti aperti, 188 completati. File 100% legacy. Archiviato in `docs/archive/TODO.md` con `git mv`. `TODO_MIGLIORAMENTI.md` è l'unico TODO attivo.

### `.dockerignore` — lacune
- [x] **`.dockerignore` espanso**. Già completato (51 righe): coverage, logs, .github, .vscode, .claude, TODO*.md, AUDIT*.md, lint.*, .prettierrc, eslint.config.js, vitest.config.ts, n8n/. Originale era 98 bytes, 9 righe: `.git`, `node_modules`, `dist`, `data`, `*.log`, `repomix-output.txt`, `.env`, `playwright-report`, `test-results`. Mancano: `coverage/`, `logs/`, `.github/`, `.vscode/`, `.claude/`, `*.md` (tranne README), `lint.json`, `lint_output.txt`, `typecheck_output.txt`, `.env.*`, `.prettierrc`, `.prettierignore`, `eslint.config.js`, `vitest.config.ts`, `tsconfig.frontend.json`, `n8n/`, `TODO*.md`, `AUDIT*.md`. Il build context copia inutilmente ~200KB di file non necessari all'immagine.

### `.gitignore` — lacune
- [x] **`.gitignore` espanso**. Già completato: `lint.json`, `lint_output.txt`, `typecheck_output.txt`, `.claude/`, `data/linkedin.db` aggiunti esplicitamente.
- [x] **Docker log rotation mancante**: in `docker-compose.yml` nessun driver logging configurato. In produzione i log JSON possono riempire il disco. Aggiungere `logging: { driver: json-file, options: { max-size: "10m", max-file: "3" } }` ai servizi bot e dashboard.
- [x] **`.dockerignore` espanso**: aggiunto: output temporanei (lint.json, lint_output.txt, typecheck_output.txt), documentazione non necessaria (TODO*.md, AUDIT*.md, ecc.), config IDE (.vscode, .claude, .github), tool config (.prettierrc, eslint.config.js, vitest.config.ts), n8n/.
- [x] **PM2 in `devDependencies`**: `pm2` è usato in produzione (`ecosystem.config.cjs`, `daemon:start`) ma è dichiarato come devDependency. `npm ci --omit=dev` lo esclude. Spostare in `dependencies` oppure affidarsi solo a Docker restart policy.
- [x] **`docker-compose.yml` `version: '3.9'` deprecato**: il campo `version` è deprecato nelle versioni recenti di Docker Compose. Rimuovere la riga.
- [x] **Deep healthcheck endpoint**: implementato `GET /api/health/deep` in `server.ts`. Verifica: (1) connettività DB (`SELECT 1`), (2) stato pausa/quarantine, (3) outbox backlog vs threshold, (4) queue depth. Ritorna 200 se tutto OK, 503 se degradato con dettaglio per ogni check.
- [ ] **Monitoring / APM assente**: nessuna integrazione con Prometheus, Grafana, Sentry o equivalenti. Esporre metriche Prometheus su `/metrics` (invites/hour, risk score, error rate, queue depth, proxy health). Aggiungere Sentry per error tracking centralizzato.
- [x] **File orfani in root da pulire**: `lint.json`, `lint_output.txt`, `typecheck_output.txt`, `data/linkedin.db`, `.claude/` aggiunti al `.gitignore`. `data/compare_extras.js` eliminato. `data/linkedin.db` non esisteva.
- [x] **`src/services/` directory vuota**: directory presente ma completamente vuota. Rimuoverla oppure usarla come target per la separazione business logic dai repository (vedi sezione 2, repository pattern).
- [x] **Backup manuali non gestiti in `data/`** — verificato: `data/backups/` è vuota (nessun backup manuale residuo). **Bug L3 fixato**: la retention policy in `backupDb.ts` filtrava solo `backup-*` ma i backup PG usano `pg_backup_*` — aggiunto pattern `pg_backup_` al filtro. Ora la retention copre sia SQLite che PG.

## 9. Findings Critici dal Secondo Passaggio (Priorità: CRITICA)
Problemi emersi dall'analisi approfondita di file precedentemente non letti. Include `.env` reale, stealth scripts completi, API routes, proxy manager.

- [x] **`.env` — configurazione documentata**: aggiunti commenti NOTA esplicativi per tutte e 4 le incoerenze: (a) `INVITE_WITH_NOTE=false` con nota sul potenziale di miglioramento; (b) header AI rinominato "cloud-first per qualità, con guardrail local-first" con nota su SECURITY.md; (c) `MULTI_ACCOUNT_ENABLED=false` con nota che le credenziali sono pronte; (d) dual sync WEBHOOK+SUPABASE documentato come intenzionale.
- [x] **Doppia patch WebGL risolta**: rimossa sezione 18 di `stealthScripts.ts` che sovrascriveva la patch di `launcher.ts`. Ora UN SOLO punto gestisce WebGL: `launcher.ts` con pool di 12 renderer realistici e selezione deterministica per fingerprint. La sezione 18 è sostituita da un commento [RIMOSSO] con motivazione.
- [x] **Fake Storage Seeder rischioso** (stealthScripts.ts sezione 14): inietta `_ga` (Google Analytics) e `_fbp` (Facebook Pixel) finti in localStorage. Problema: LinkedIn NON usa GA/Facebook Pixel sul proprio dominio. Un detector può verificare che questi cookie esistono senza il corrispondente script esterno — segnale di spoofing. Valutare rimozione o sostituzione con cookie LinkedIn-plausibili.
- [x] **Dead code in stealthScripts.ts sezione 19**: nell'iframe chrome consistency handler, le variabili `originalAppendChild` e `origAttach` sono dichiarate/referenziate ma mai utilizzate. Codice morto che aggiunge complessità senza beneficio.
- [x] **Route API duplicate legacy/v1**: estratti 3 helper condivisi (`handlePauseAction`, `handleResumeAction`, `handleQuarantineAction`) in `server.ts`. Le 6 route (3 legacy + 3 v1) sono ora thin wrapper che differiscono solo per source e formato risposta.
- [x] **proxyManager.ts — DRY violation**: `getProxyPoolStatus()` e `getIntegrationProxyPoolStatus()` sono funzioni quasi identiche (solo il registry cooldown cambia). Estrarre una funzione parametrica `getPoolStatus(registry)` e riutilizzarla.
- [x] **`humanType` — typing speed context-aware per lunghezza testo**: implementato `lengthSlowFactor` in `humanBehavior.ts`: testi brevi (≤30 char) ×0.85 (più veloce), medi (30-150) ×1.0, lunghi (150-400) ×1.15, molto lunghi (>400) ×1.3. Il factor moltiplica il delay base bimodale per ogni carattere, simulando affaticamento naturale.

## 10. Duplicazioni Cross-File, File Morti e Struttura (Priorità: MEDIA)
Risultati della ricerca sistematica con grep su tutta la codebase per funzioni duplicate, file orfani e inconsistenze strutturali.

### Utility duplicate (stessa funzione reimplementata in più file)
- [x] **`randomInt()` duplicata in 3 file**: `humanBehavior.ts`, `organicContent.ts`, `jobRunner.ts` — stessa firma `function randomInt(min, max)`. Estrarre in `src/utils/random.ts` e importare.
- [x] **`randomElement()` duplicata in 3 file**: `humanBehavior.ts`, `organicContent.ts`, `stealth.ts` — stessa firma. Estrarre in utility condivisa.
- [x] **`pickRandom()` duplicata in 5 file**: `postContentGenerator.ts`, `scheduler.ts`, `fingerprint/pool.ts`, `abBandit.ts`, `randomActivityWorker.ts`. Unificato: `pickRandomVariant` → `randomElement`, `pickRandomInt` → `randomInt`, `pickRandomTopic`/`pickTemplatePost` usano `randomElement` internamente.
- [x] **`cleanText()` duplicata in 5 file**: `salesNavigatorSync.ts`, `personDataFinder.ts`, `bulkSaveOrchestrator.ts`, `listActions.ts`, `listScraper.ts` — tutte fanno `.replace(/\s+/g, ' ').trim()`. Estrarre in utility.
- [x] **`retryDelayMs()` duplicata in 3 file**: già estratta in `src/utils/async.ts` e importata da `jobRunner.ts`, `supabaseSyncWorker.ts`, `webhookSyncWorker.ts`.
- [x] **`sleep()` duplicata in 2 file**: `loopCommand.ts`, `integrationPolicy.ts` — stessa `new Promise(resolve => setTimeout(resolve, ms))`. Ora importano da `src/utils/async.ts`.
- [x] **`parseCsvEnv()` duplicata in 2 file**: `config/env.ts` e `plugins/pluginLoader.ts` — stessa logica CSV split+trim+filter. Il pluginLoader dovrebbe importare da `config/env.ts`.

### Doppio fingerprint pool (struttura confusa)
- [x] **Due file `pool.ts` consolidati**. `src/browser/fingerprint/pool.ts` (55 righe, `FingerprintPool` class noise generation) → spostato in `src/fingerprint/noiseGenerator.ts`. Tipo parametro cambiato da `BrowserFingerprint` a `Fingerprint` (tipo base — usa solo campi base: `userAgent`, `id`, `viewport`). Import in `launcher.ts` aggiornato da `./fingerprint/pool` a `../fingerprint/noiseGenerator`. Directory vuota `src/browser/fingerprint/` rimossa. Ora tutti i moduli fingerprint sono in `src/fingerprint/`: `pool.ts` (dati) + `noiseGenerator.ts` (noise).

### File morti confermati
- [x] **`data/compare_extras.js`** — eliminato (file morto, mai referenziato).
- [x] **`data/linkedin.db`** — non esisteva (già documentato nel `.gitignore`).

## 11. Pulizia Root, Igiene Repository e Coerenza File (Priorità: MEDIA)
Analisi sistematica di ogni file in root e della struttura del repository per rimuovere il rumore, allineare la documentazione, e rendere il progetto professionale.

### File root da rimuovere o spostare
- [x] **`lint.json`, `lint_output.txt`, `typecheck_output.txt`**: già aggiunti al `.gitignore` (righe 35-37).
- [x] **`.env.bak` (4.7KB)** — verificato: NON tracked in git (`git ls-files .env.bak` = vuoto). Il `.gitignore` copre già il pattern. Nessuna azione necessaria.
- [x] **`repomix.config.json`** — aggiornato: `INTEGRATIONS.md` → `docs/INTEGRATIONS.md`. Include già `TODO_MIGLIORAMENTI.md` e `SECURITY.md`. `ROADMAP.md` non presente (già rimosso in sessione precedente).
- [x] **`.claude/` directory**: già aggiunto `.claude/` al `.gitignore` (riga 43).

### Documentazione root — consolidamento
- [x] **Troppi `.md` in root — consolidati in `docs/`**. Spostati con `git mv`: `AI_QUALITY_PIPELINE.md` → `docs/`, `CONFIG_REFERENCE.md` → `docs/`, `INTEGRATIONS.md` → `docs/`. Aggiornati tutti i riferimenti: `README.md` (3 link + tree struttura), `repomix.config.json` (path INTEGRATIONS), `scripts/generate-config-docs.mjs` (output path), `.dockerignore` (aggiunta `docs/` + `README.md`). In root restano solo: `README.md`, `SECURITY.md`, `THREAT_MODEL.md`, `GUIDA_ANTI_BAN.md`, `TODO_MIGLIORAMENTI.md`.
- [x] **`AUDIT_COMPLETAMENTI_2026.md` (39KB)** — spostato in `docs/archive/AUDIT_COMPLETAMENTI_2026.md` con `git mv`. File storico, non operativo.
- [x] **`TODO.md` (75KB)** — verificato: 0 punti aperti, 188 completati. File 100% legacy. Archiviato in `docs/archive/TODO.md` con `git mv`. `TODO_MIGLIORAMENTI.md` è l'unico TODO attivo.

### `.dockerignore` — lacune
- [x] **`.dockerignore` espanso**. Già completato (51 righe): coverage, logs, .github, .vscode, .claude, TODO*.md, AUDIT*.md, lint.*, .prettierrc, eslint.config.js, vitest.config.ts, n8n/. Originale era 98 bytes, 9 righe: `.git`, `node_modules`, `dist`, `data`, `*.log`, `repomix-output.txt`, `.env`, `playwright-report`, `test-results`. Mancano: `coverage/`, `logs/`, `.github/`, `.vscode/`, `.claude/`, `*.md` (tranne README), `lint.json`, `lint_output.txt`, `typecheck_output.txt`, `.env.*`, `.prettierrc`, `.prettierignore`, `eslint.config.js`, `vitest.config.ts`, `tsconfig.frontend.json`, `n8n/`, `TODO*.md`, `AUDIT*.md`. Il build context copia inutilmente ~200KB di file non necessari all'immagine.

### `.gitignore` — lacune
- [x] **`.gitignore` espanso**. Già completato: `lint.json`, `lint_output.txt`, `typecheck_output.txt`, `.claude/`, `data/linkedin.db` aggiunti esplicitamente.

## 12. Coerenza Comandi CLI e Workflow (Priorità: MEDIA)
Verifica sistematica che tutti i comandi CLI documentati nel help siano implementati, che gli alias deprecati siano gestiti coerentemente, e che i workflow siano completi.

### Comandi deprecati — pulizia
- [x] **10 alias deprecati — data rimozione v2.0 aggiunta**. Warning aggiornato: `"${command}" è deprecato e verrà rimosso in v2.0`. `--strict` li rifiuta. `salesnav-extract-search` e `salesnav-extract-first-search` già rimossi.
- [x] **`browserCommands` Set allineato con deprecati — BUG FIXATO**. Mancavano 4 alias deprecati che richiedono browser: `salesnav-lists`, `salesnav-create-list`, `salesnav-add-lead`, `salesnav-add-to-list`. Senza questi, il proxy health check (riga 376) veniva saltato per quei comandi. Ora tutti i 10 alias deprecati sono nel Set.

### Comandi non documentati nel help
- [x] **`db-analyze` non documentato**. Il comando `db-analyze` ha un handler (riga 532-534) e un'implementazione in `adminCommands.ts` (riga 473), ma NON appare nel `printHelp()` (righe 131-204). L'utente non sa che esiste. Aggiungerlo all'help.
- [x] **`daily-report` non documentato**. Il comando `daily-report` (riga 574) genera e invia il report giornaliero. Non appare nell'help. Aggiungerlo.
- [x] **`repl` non documentato**. Il comando `repl` (riga 582) avvia una console interattiva. Non appare nell'help. Aggiungerlo.
- [x] **`warmup` non documentato**. Il comando `warmup` (riga 571) esegue il workflow warmup. Non appare nell'help come comando standalone (appare solo come sub-workflow di `run`). Aggiungerlo o documentare che va usato via `run warmup`.
- [x] **`dashboard` non documentato esplicitamente**. Il comando `dashboard` (riga 577) avvia il server Express. Non appare nell'help CLI (c'è solo in `package.json` scripts). Aggiungerlo.

### Workflow production — coerenza
- [x] **4 workflow production vs comandi run**: già documentato nell'help CLI (`printHelp` in `index.ts`). Sezione "Production Workflows (pre-flight interattivo + enrichment + report)" per sync-list/sync-search/send-messages/send-invites, separata da "Comandi principali (job queue)" per run invite/check/message/all.

## 13. Performance, Velocità e Ottimizzazioni Anti-Ban Safe (Priorità: ALTA)
Metodi per velocizzare il bot SENZA aumentare il rischio di ban. Il principio: ottimizzare il tempo NON speso su LinkedIn, non il tempo speso su LinkedIn.

### Velocizzare operazioni OFF-LinkedIn (zero rischio ban)
- [x] **Enrichment parallelo — GIÀ INTEGRATO**. `sendInvitesWorkflow.ts` righe 197-211: esegue `enrichLeadsParallel({ concurrency: 5 })` prima degli inviti con progress bar. `syncListWorkflow` e `syncSearchWorkflow` hanno parametro `enrichment` (default true, disabilitabile con `--no-enrich`). Inoltre: `jobRunner.ts` ora lancia micro-enrichment (limit:2) in parallelo con `interJobDelay` ad ogni job (Sez.16 FASE 3).
- [ ] **Pre-build dei messaggi AI in batch**. Attualmente `messagePersonalizer` genera il messaggio AI al momento dell'invio — durante la sessione browser con il timer che scorre. Pre-generare i messaggi offline (senza browser) in un batch job separato. Quando il worker li invia, il messaggio è già pronto. Risparmio: ~2-5 secondi per messaggio × N messaggi = minuti di sessione browser in meno. Meno tempo con browser aperto = meno esposizione.
- [x] **Startup più veloce**. Fast path in `applyMigrations()`: se tutte le migrazioni sono già applicate (`pendingFiles.length === 0`), early return senza leggere file SQL né eseguire ensureColumn. Risparmio per boot: ~50 `fs.readFileSync` + ~20 `ALTER TABLE` idempotenti. Le ensureColumn vengono eseguite SOLO quando ci sono nuove migrazioni (primo boot o aggiornamento). Edge case coperti: DB nuovo (tutte pending → loop completo), aggiornamento (solo nuove → loop + ensureColumn), boot normale (skip totale).
- [x] **SQL normalization cache**: già implementato (vedi sez.2). `sqlCache` Map con `SQL_CACHE_MAX=500` in `PostgresManager`.

### Velocizzare operazioni SU LinkedIn (rischio basso se fatto bene)
- [x] **Navigazione pagine già uniformata a `domcontentloaded`**. Verificato: 55 su 56 `goto()` usano `domcontentloaded`. L'unico `'load'` è in `utilCommands.ts` per la pagina login — corretto perché il form deve caricare completamente.
- [x] **Ridurre screenshot inutili**. Aggiunto early-exit `isStillOnChallengePage(page)` in `challengeHandler.ts` PRIMA dello screenshot costoso (~200ms + API call vision). Ad ogni iterazione del loop (attempt 1 e 2), verifica che il challenge sia ancora presente prima di fare `page.screenshot()`. Se il challenge è già risolto (race condition con page load asincrono, o risolto tra i tentativi), ritorna `true` senza screenshot. Risparmio: ~200ms + costo API vision per ogni tentativo non necessario.
- [x] **Parallelizzare operazioni durante delay — (b) COMPLETATO**. `jobRunner.ts`: `Promise.allSettled([interJobDelay(...), enrichLeadsParallel({ limit: 2, concurrency: 1 })])` dopo ogni job riuscito. Punto (b) enrichment implementato. Punti (c) sync Supabase e (d) cleanup outbox restano come miglioramenti futuri (già eseguiti nel loop tra i cicli, non urgenti).
- [x] **Browser GC già automatico**: `jobRunner.ts` riga 719 esegue `performBrowserGC(session)` ogni 10 job (`processedOnCurrentSession % 10 === 0`). Già implementato nella codebase esistente.

### Anti-ban improvements — velocità come DIFESA
- [x] **Sessioni più corte**: già configurato. `INTER_JOB_MAX_DELAY_SEC=180` nel `.env` (non 300 come descritto nella TODO originale). Con `WARMUP_TWO_SESSIONS_PER_DAY=true` → 2 sessioni da ~25 min.
- [x] **Budget split 60/40 mattina/pomeriggio**. `getSessionBudgetFactor()` in `sessionWarmer.ts` ora ritorna 0.6 per `first` e 0.4 per `second` (prima era 0.5/0.5). La mattina ha tipicamente acceptance rate più alto → più budget alla sessione mattutina.
- [x] **Skip profili già visitati nel giorno**. Implementato `visitedProfilesToday: Set<string>` nel `WorkerContext` (opzionale, retrocompatibile). Creato in `jobRunner.ts` prima del loop job, condiviso tra tutti i worker. In `inviteWorker.ts`: URL normalizzato (lowercase, no trailing slash), check `has()` prima di navigare, `add()` dopo. Se già visitato → `workerResult(0)` (skip silenzioso, lead resta READY_INVITE per il giorno dopo).
- [x] **Randomizzare l'ordine di processing dei job**. Implementato in `repositories/jobs.ts` `lockNextQueuedJob()`: `ORDER BY priority ASC, scheduled_at + (ABS(RANDOM()) % 60)` per SQLite, `scheduled_at + (random() * interval '60 seconds')` per PG. Shuffle ±60s sull'ordine senza rompere la priorità temporale. Il pattern FIFO costante non è più rilevabile.
- [x] **Adaptive inter-job delay basato su risposta server**. Implementato: `interJobDelay()` accetta `throttleSignal` opzionale, `jobRunner.ts` passa `session.httpThrottler.getThrottleSignal()` ad ogni ciclo. `shouldSlow` → delay ×1.5, `shouldPause` → coffee break 3-5 min. Feedback loop reattivo completato.

## 14. Strategie Avanzate Cross-Modulo e Anti-Detection Intelligente (Priorità: ALTA)
Collegamenti tra moduli diversi che singolarmente funzionano ma insieme creano pattern rilevabili, più strategie furbe per aumentare l'acceptance rate (meno pending = meno rischio ban).

### Moduli esistenti non collegati tra loro (potenza sprecata)
- [x] **`httpThrottler` collegato al sistema di delay**. `interJobDelay()` in `humanBehavior.ts` accetta ora `throttleSignal?: { shouldSlow, shouldPause }`. In `jobRunner.ts`, `session.httpThrottler.getThrottleSignal()` viene passato a ogni `interJobDelay()`. Se `shouldSlow` → delay ×1.5. Se `shouldPause` → coffee break forzata 3-5 min. Feedback loop reattivo: LinkedIn rallenta → bot rallenta automaticamente.
- [x] **`weeklyStrategyEnabled=true` nel `.env` — strategia settimanale abilitata**. `WEEKLY_STRATEGY_ENABLED=true` già configurato nel `.env` attuale. Lo `scheduler.ts` riceve i fattori giornalieri da `strategyPlanner.ts` (lunedì: alto inviti, giovedì: focus messaggi, venerdì: wind-down). Pattern settimanale umano attivo.
- [x] **`sessionMemory.pacingFactor` collegato a interJobDelay**. `getSessionHistory()` viene chiamato in `jobRunner.ts` prima del loop job. Il `pacingFactor` (0.5-1.2) viene passato a `interJobDelay()` come terzo parametro. Logica: `totalDelay / pacingFactor` — dopo challenge recenti (factor 0.5) il delay raddoppia, dopo giorni tranquilli (factor 1.1) diminuisce leggermente. Complementare a httpThrottler (che reagisce in tempo reale) e al mood factor (che varia per data).
- [ ] **`timingOptimizer` e `abBandit` non comunicano**. Il timing optimizer (`timingOptimizer.ts`, 498 righe) decide QUANDO inviare l'invito (quale ora/giorno). L'AB bandit (`abBandit.ts`, 504 righe) decide COME inviare (quale variante di messaggio). Ma non si incrociano: lo stesso messaggio viene inviato alla stessa ora indipendentemente dalla variante. Collegamento avanzato: certe varianti di messaggio funzionano meglio a certe ore (es. messaggi formali la mattina, informali il pomeriggio). Cross-segmentare timing × variante nell'AB test.

### Anti-detection: pattern cross-sessione che LinkedIn rileva
- [x] **Login hour jitter implementato**. In `loopCommand.ts`, delay random 0-30 min prima del primo ciclo (`Math.random() * 30 * 60 * 1000`). Il bot non fa più login alle 09:00:00 sharp ogni giorno. Implementato nella sez.16 FASE 1.
- [x] **Durata sessione variabile**. Implementato daily mood factor in `scheduler.ts`: FNV-1a hash della data genera un factor [0.80, 1.20] deterministico per giorno. Il budget inviti e messaggi varia ±20% tra giorni. La media settimanale resta invariata ma ogni giorno ha durata diversa.
- [x] **Ratio inviti/messaggi variabile tra giorni**. Implementato ratio mood shift in `scheduler.ts`: secondo hash FNV-1a genera un shift [-0.15, +0.15] che sbilancia invite vs message indipendentemente dalla durata. Es. oggi invite ×1.15 e message ×0.85, domani il contrario. Deterministico per data, complementare al weekly strategy.
- [x] **Pagine visitate con ordine variabile**. Implementato in `inviteWorker.ts`: 20% di probabilità di navigare a `/recent-activity/all/` del target, `simulateHumanReading()`, poi ritorno al profilo prima di cliccare Connect. Rende la visita più organica. Fallback silenzioso se la pagina non carica (`.catch(() => null)`).

### Strategie furbe per massimizzare acceptance rate (meno pending = meno ban)
- [ ] **"Warm touch" prima dell'invito**. LinkedIn traccia le interazioni pre-invito. Se PRIMA di invitare qualcuno il bot: (a) visualizza il profilo del target 1-2 giorni prima (profile view), (b) mette Like a un suo post, (c) segue il target — l'acceptance rate sale del 30-50% perché il target vede "chi ha visitato il tuo profilo" e riconosce il nome quando arriva l'invito. Il sistema drip campaign (`campaignEngine.ts`) supporta già step `VIEW_PROFILE`, `LIKE_POST`, `FOLLOW` — ma il workflow default `send-invites` NON li usa. Creare un workflow "warm-touch → wait 1-2 days → invite" come template consigliato.
- [ ] **Like strategici sui post del target (non random)**. `organicContent.ts` mette Like su post random nel feed. Ma un Like su un post del target specifico che stai per invitare è molto più efficace di un Like random. Modificare: in `interactionWorker.ts` quando `actionType='LIKE_POST'`, navigare al profilo del lead target → activity → Like il post più recente. Il target vede la notifica "X ha reagito al tuo post" e poi riceve l'invito da X — coincidenza apparente che aumenta la fiducia.
- [ ] **Inviti con nota personalizzata basata su contesto reale**. L'acceptance rate con nota personalizzata è 30-40% vs 15-20% senza nota. Ma `INVITE_WITH_NOTE=false` nel `.env` attuale — un'opportunità persa. Con `AI_PERSONALIZATION_ENABLED=true` (già attivo) e `INVITE_NOTE_MODE=ai`, il bot genera note personalizzate via GPT-5.4. Abilitare `INVITE_WITH_NOTE=true` e `INVITE_NOTE_MODE=ai`. Costo: ~$0.002 per nota. Beneficio: raddoppio del tasso di accettazione → dimezzamento del pending ratio → dimezzamento del rischio ban.
- [x] **Targeting intelligente: già implementato**. `getLeadsByStatusForList()` in `leadsCore.ts` ordina per `COALESCE(lead_score, -1) DESC, created_at ASC` — i lead con score più alto vengono selezionati per primi. Il budget giornaliero va automaticamente ai lead più promettenti.
- [x] **Ritirare inviti pending vecchi**: già configurato. `PENDING_INVITE_MAX_DAYS=21` nel `.env` (non 30 come descritto nella TODO). Il `hygieneWorker.ts` ritira automaticamente dopo 21 giorni.

### Ottimizzazioni di intelligence operativa
- [x] **Auto-pausa maintenance window LinkedIn**. Implementato in `loopCommand.ts`: all'inizio di ogni ciclo, se `new Date().getHours()` è tra 3 e 5 (03:00-06:00 locale), il ciclo viene skippato con log `maintenance_window_skip` e attesa 15 min prima di ri-controllare. Previene errori inutili durante i deploy notturni LinkedIn.
- [ ] **Correlazione cross-account**. Con `MULTI_ACCOUNT_ENABLED`, due account sullo stesso proxy Oxylabs o sulla stessa macchina creano una correlazione. LinkedIn può vedere che due account fanno login dallo stesso IP nello stesso minuto. Fix: sfasare le sessioni di almeno 2-3 ore tra account. Se account1 lavora 9-11, account2 lavora 14-16. Mai sovrapposizione. Verificare in `loopCommand.ts` che i due account non siano mai in sessione contemporanea.
- [x] **Tracciare le "impressioni" per ROI**. Migration `052_engagement_stats.sql` aggiunge colonne `likes_given` e `follows_given` a `daily_stats`. `profile_views` già esisteva (migration 044). Tipo union di `incrementDailyStat`/`getDailyStat` aggiornato in `stats.ts`. `interactionWorker.ts`: `LIKE_POST` ora incrementa `likes_given`, `FOLLOW` incrementa `follows_given`. L'utente può ora calcolare il funnel completo: profile_views → likes → follows → invites → acceptances → messages.

## 15. Cosa Aggiungere, Cosa Togliere, Cosa Ripensare (Priorità: STRATEGICA)
Oltre a migliorare l'esistente: cosa **non esiste e dovrebbe**, cosa **esiste e fa danno**, cosa **esiste ma con l'approccio sbagliato**.

### DA TOGLIERE — Codice che crea più rischio che valore

- [x] **TOGLIERE: Fake Storage Seeder (stealthScripts.ts sezione 14)**. Inietta `_ga`, `_fbp` in localStorage e finti DB IndexedDB (`localforage`, `firebaseLocalStorageDb`). LinkedIn non usa GA/Facebook Pixel — un detector vede cookie senza script corrispondente = spoofing ovvio. I finti IndexedDB sono ancor peggio: `firebaseLocalStorageDb` esiste solo se Firebase SDK è caricato, cosa che LinkedIn non fa. Eliminare l'intera sezione 14. Se si vuole popolare localStorage per non apparire "vuoti", usare solo chiavi generiche e innocue (`theme_preference`, `lang`, `cookie_consent_accepted`).
- [x] **TOGLIERE: CycleTLS proxy module — RIMOSSO**. Eliminati: `src/proxy/cycleTlsProxy.ts` (204 righe), `src/types/cycletls.d.ts`. Rimossi 8 punti di contatto: import + 3 `stopCycleTlsProxy` calls in `index.ts` (graceful shutdown, planned restart, finally block), `startCycleTlsProxy` boot call, import + `ensureCycleTlsProxy` + `stopCycleTlsProxy` in `launcher.ts`. Il blocco `if (config.useJa3Proxy)` in launcher rimosso — il proxy path ora usa direttamente `currentProxy`. Config fields `useJa3Proxy`, `ja3ProxyPort` etc. mantenuti in types.ts per retrocompatibilità .env.
- [x] **TOGLIERE: Voice Commands dashboard — RIMOSSO**. Eliminati: `src/frontend/voiceCommands.ts` (116 righe), ~130 righe in `main.ts` (import, 8 interfaces SpeechRecognition, 3 variabili state, 10 funzioni voice, chiamata bindVoiceControls). Test voice rimossi da `unit.vitest.ts`. main.ts da ~1009 a ~770 righe (-24%). Keyboard shortcuts (`bindKeyboardShortcuts`) mantenuti come alternativa superiore.
- [x] **VALUTATO: `src/browser/deviceProfile.ts` MANTENUTO come modulo separato**. Analisi 360°: ha 3 consumatori distinti (`humanBehavior.ts` → `isMobilePage`, `launcher.ts` → `registerPageDeviceProfile` + `DeviceProfile`, barrel `browser/index.ts` → re-export). Il `WeakMap<Page, DeviceProfile>` è uno stato condiviso globale — se inline-ato in `launcher.ts`, `humanBehavior.ts` dovrebbe importare da `launcher.ts` creando una dipendenza circolare pesante. 31 righe con responsabilità chiara = modulo legittimo.

### DA RIPENSARE — Approccio sbagliato da rifare diversamente

- [x] **RIPENSARE: Doppia patch WebGL — RISOLTA**. Rimossa sezione 18 da `stealthScripts.ts` che sovrascriveva la patch di `launcher.ts`. Ora `launcher.ts` è l'UNICO punto che gestisce WebGL con pool di 12 renderer realistici e selezione deterministica per fingerprint. La sezione stealth 18 è sostituita da commento [RIMOSSO]. Nessuna doppia iniezione, nessuna incoerenza.
- [ ] **RIPENSARE: Il dual-DB adapter pattern (SQLiteManager / PostgresManager)**. L'idea era buona (supportare entrambi i DB) ma l'implementazione via `normalizeSql()` con 8 regex è fragile. Ogni nuova query che usa una funzione SQLite-specifica (es. `json_set`, `GROUP_CONCAT`, `SUBSTR`) richiede una nuova regex. E non tutte le conversioni sono testabili — `json_set` in SQLite vs `jsonb_set` in PG hanno semantiche diverse sui NULL. **Ripensare**: scegliere UN database target per la produzione (PG) e mantenere SQLite SOLO per dev/test con un subset di funzionalità. Oppure usare Kysely che genera SQL dialect-aware e rende il problema inesistente. L'attuale approccio "scrivi SQLite, converto on-the-fly" è un debito tecnico che cresce con ogni migrazione.
- [ ] **RIPENSARE: God files come architettura accettata**. `server.ts` (1603 righe), `bulkSaveOrchestrator.ts` (112KB), `adminCommands.ts` (43KB), `loopCommand.ts` (32KB), `utilCommands.ts` (34KB), `salesNavCommands.ts` (30KB). Sono 6 file che totalizzano ~300KB. Il pattern è sempre lo stesso: un file inizia piccolo, cresce incrementalmente, e nessuno lo splitta perché "funziona". Il problema non è solo la leggibilità — è che un bug in un file da 1600 righe è 10x più difficile da isolare, testare e revieware. **Regola**: nessun file `.ts` dovrebbe superare le 500 righe. Quando raggiunge 400, è il momento di estrarre.
### DA AGGIUNGERE — Feature che non esistono e dovrebbero

- [x] **AGGIUNGERE: LinkedIn API monitoring passivo**. Implementato: `probeLinkedInStatus()` in `browser/auth.ts` naviga a `/feed/` e verifica HTTP status, login, challenge, response time. Integrato in `jobRunner.ts` dopo `checkLogin()`: se 429 → pausa automatica, se sessione scaduta → quarantine, se challenge → handleChallengeDetected, se slow → warning e procedi con cautela. Probe eseguita prima di ogni batch di job.
- [x] **AGGIUNGERE: Circuit breaker per sessione browser**. Implementato in `jobRunner.ts`: quando `consecutiveFailures >= maxConsecutiveJobFailures`, forza `rotateSessionWithLoginCheck()` PRIMA di `pauseAutomation()`. La rotazione chiude il browser, ne apre uno nuovo con fingerprint diverso e (potenzialmente) proxy diverso. Al resume il bot ha una chance reale di successo. Se la rotazione fallisce (login missing), `quarantineAccount` viene settato. Log include `sessionRotated: boolean`.
- [x] **AGGIUNGERE: Inbox monitoring come segnale anti-ban**. `inboxWorker.ts` righe 100-132: scansiona le prime 8 conversazioni per keywords di sistema LinkedIn (`unusual activity`, `restricted`, `verify your identity`, `temporarily limited`, `attività insolita`, `account limitato`, `verifica la tua identità`). Se trovato: `pauseAutomation('LINKEDIN_INBOX_WARNING')` per 24h + `sendTelegramAlert` critical + early return. Implementazione completa con import dinamici e best-effort error handling.
- [ ] **AGGIUNGERE: SSI (Social Selling Index) scraping automatico**. Il `.env` ha `SSI_DYNAMIC_LIMITS_ENABLED=true` e `SSI_STATE_KEY=linkedin_ssi_score` ma il valore SSI viene letto da `sync_state` — chi lo scrive? Nessuno automaticamente. L'utente deve aggiornarlo manualmente. Aggiungere: un task periodico (1x/settimana) che naviga a `https://www.linkedin.com/sales/ssi` e scrappa il punteggio SSI. Aggiornare `sync_state[linkedin_ssi_score]` automaticamente. Il budget dinamico basato su SSI funziona solo se il dato è aggiornato.
- [x] **AGGIUNGERE: Blacklist di profili/company da non contattare**. Implementato: migrazione DB `051_blacklist.sql` (tabella + indici), repository `repositories/blacklist.ts` con `isBlacklisted()`, `addToBlacklist()`, `removeFromBlacklist()`, `listBlacklist()`, `countBlacklist()`. Check preventivo integrato in `scheduler.ts` prima di `enqueueJob('INVITE')` E `enqueueJob('MESSAGE')`. API REST: `GET/POST /api/blacklist`, `DELETE /api/blacklist/:id`. Normalizzazione URL e dominio per matching coerente.
- [ ] **AGGIUNGERE: Dashboard "what-if" simulator**. Nella dashboard, aggiungere un pannello dove l'utente inserisce: "cosa succede se aumento HARD_INVITE_CAP a 20?" e il sistema mostra l'impatto previsto: risk score stimato, weekly invites stimati, pending ratio previsto, compliance violations. Usa le funzioni già esistenti (`evaluateRisk`, `calculateDynamicBudget`, `evaluateComplianceHealthScore`) con parametri ipotetici. Zero rischio — è solo una simulazione.
- [x] **AGGIUNGERE: Export automatico lead "hot" via Telegram**. Implementato in `inboxWorker.ts`: quando `intent === 'POSITIVE' || intent === 'QUESTIONS'` con `confidence >= 0.8`, invia `sendTelegramAlert` immediato con nome, azienda, email, URL LinkedIn, ed estratto del messaggio (max 100 char). L'utente riceve la notifica "Lead caldo" in tempo reale.
- [ ] **AGGIUNGERE: Proxy health dashboard in real-time**. `proxyManager.ts` (734 righe) traccia ready/cooling/failed ma queste info sono visibili solo via CLI (`proxy-status`). Aggiungere un widget nella dashboard che mostra: proxy attivi, proxy in cooldown (con countdown), proxy falliti, ultimo health check, response time medio del proxy. L'endpoint `/api/observability` ha già `getProxyPoolStatus()` — serve solo il rendering frontend.
- [x] **AGGIUNGERE: Security advisor periodico**. `securityAdvisor.ts` genera report JSON in `data/security-advisor/security-advisor-{timestamp}.json`. Persiste 7 runtime_flags (`security_advisor_last_run_at`, `_last_status`, `_last_reason`, `_last_report_path`, `_last_findings_count`, `_last_backlog_count`, `_last_error`). Pubblica outbox event `security.advisor.report`. Record audit event. Posture check (`getSecurityAdvisorPosture`) rileva: never executed, stale, last failed, last warn.
- [x] **AGGIUNGERE: Confidence check pre-click su Connect**. In `inviteWorker.ts`, prima di cliccare il bottone Connect, verifica che il testo contenga "Connect"/"Collegati"/"Connetti". Se il selettore matcha un bottone sbagliato (es. "Report" o "Follow"), il click non avviene e il lead viene skippato. Check su bottone primario E menu item. Previene azioni irreversibili su bottoni sbagliati quando LinkedIn cambia il layout.

## 16. Best Practice Operative End-to-End (Priorità: ALTA)
Il flusso completo dall'accensione del PC allo spegnimento. Ogni fase ha un razionale anti-ban e un check da implementare nel codice o nella documentazione.

### FASE 0 — Prima di accendere (setup una tantum)
- [x] **Checklist ambiente — `preflight-env` implementato**. Comando CLI `preflight-env` in `cli/commands/preflightEnv.ts` con 5 check: (1) spazio disco data/ (FAIL <1GB, WARN <5GB), (2) proxy raggiungibile per ogni account (checkProxyHealth), (3) Ollama raggiungibile se configurato (fetch /api/tags con timeout 5s), (4) session directory esistenti, (5) database accessibile (SELECT 1). Output con emoji ✅⚠️❌. Exit code 1 se FAIL. Dynamic import in index.ts.
- [x] **Profilo LinkedIn "caldo" — documentato in GUIDA_ANTI_BAN.md**. Sezione 10 ampliata: 1 settimana di attività manuale obbligatoria prima del bot (profilo completo, feed scroll, like, connessioni manuali, post). Spiegato il razionale: account freddo → 20 inviti/giorno = red flag immediato. Ramp-up graduale dopo la settimana manuale (5-10 → 15-20 → pieno). `GROWTH_MODEL_ENABLED=true` gestisce il ramp-up automatico del bot.

### FASE 1 — Accensione e boot del bot
- [x] **Login jitter**: prima del primo ciclo, aggiungere un delay random 0-30 minuti per non avere login alle 09:00:00 sharp ogni giorno. In `loopCommand.ts`, prima di `acquireWorkflowRunnerLock()`: `await sleep(Math.floor(Math.random() * 30 * 60 * 1000))`. Il `sessionMemory.ts` traccia `login_hour` — variare l'orario rende il pattern meno rilevabile.
- [x] **Health check proattivo bloccante**: verificato. `MANDATORY_PREFLIGHT_ENABLED=true` esegue `runDoctor()` con 5 check bloccanti (DB integrity, login, account isolation, quarantine, compliance). Se qualsiasi fallisce → `process.exit(1)`. Proxy health check separato (checkProxyHealth per ogni account). Session freshness verificata in `jobRunner.ts`. Tutti e 3 sono bloccanti.
- [x] **Session warmup — FIXATO: ora eseguito ad ogni ciclo**. Aggiunto task `session_warmup` (task #13b) in `loopCommand.ts` PRIMA del workflow principale (#14). Condizioni: non dry-run, workflow è invite/message/all, non nel gap pranzo (`getSessionWindow() !== 'gap'`). Apre browser dedicato, esegue `warmupSession()` (feed scroll, notifiche, search simulation, messaging check per sessione 2), chiude browser. `onError: 'skip'` → non blocca il workflow se fallisce. Il warmup in `auto_site_check` resta come bonus periodico. **Anti-ban critico**: senza warmup ogni sessione partiva "a freddo" — pattern da bot.

### FASE 2 — Sessione operativa mattutina (es. 9:00-12:00)
- [x] **Ordine azioni — VERIFICATO**. `strategyPlanner.ts` ha i fattori corretti: lunedì inviteFactor=1.2, giovedì messageFactor=1.3, venerdì wind-down 0.5/0.5, sabato minimal, domenica zero. `scheduler.ts` righe 478-489 applica i fattori al budget. `WEEKLY_STRATEGY_ENABLED=true` nel .env. L'ordine azioni nel workflow è gestito dal tipo job (ACCEPTANCE_CHECK → INVITE → MESSAGE) nello scheduler, non dal loop.
- [ ] **Monitoraggio durante la sessione**: la dashboard (`http://localhost:3000`) mostra in real-time: risk score, pending ratio, inviti/messaggi odierni, incidenti aperti. Implementare: un widget "session timer" che mostra da quanto tempo il browser è aperto — se supera 45 minuti, suggerire di chiudere la sessione. Nessun umano sta su LinkedIn per più di 45 minuti di fila.
- [x] **Pausa pranzo naturale — VERIFICATA**. `getSessionWindow()` ritorna `'gap'` durante il gap 2h tra le sessioni. `getSessionBudgetFactor()` ritorna 0 → scheduler produce budget zero → nessun job. Il task `session_warmup` (#13b) fa skip su `window === 'gap'`. Il run-loop gestisce il gap correttamente: ciclo continua ma non esegue azioni operative.

### FASE 3 — Sessione operativa pomeridiana (es. 14:00-17:00)
- [x] **Secondo warmup**: coperto dal task `session_warmup` (#13b) aggiunto in `loopCommand.ts`. Esegue `warmupSession()` ad ogni ciclo — la funzione ha già logica differenziata: `sessionWindow === 'second'` (riga 121) controlla messaging tab (simula "torno a controllare risposte"). Il gap pranzo è rispettato (`getSessionWindow() === 'gap'` → skip).
- [x] **Follow-up e messaggi — VERIFICATO**. `followUpWorker.ts` è integrato nel `jobRunner.ts` come tipo job. Il `weeklyStrategyPlanner` assegna `messageFactor: 1.3` il giovedì (confermato in `strategyPlanner.ts` riga 31). La sessione pomeridiana (`sessionWindow === 'second'`) ha budget 40% (vs 60% mattina) — coerente con il principio di concentrare i follow-up nel pomeriggio quando i lead hanno avuto tempo di leggere.
- [x] **Enrichment parallelo durante i delay — IMPLEMENTATO**. In `jobRunner.ts`, dopo ogni job riuscito: `Promise.allSettled([interJobDelay(...), enrichLeadsParallel({ limit: 2, concurrency: 1 })])`. L'enrichment gira in parallelo al delay (30-180s) senza impattare il browser. Dynamic import lazy. Best-effort: se fallisce, il delay continua normalmente. Zero traffico LinkedIn — solo DNS/HTTP verso siti aziendali.

### FASE 4 — Fine giornata e shutdown
- [x] **humanWindDown**: già implementato in `launcher.ts` con distribuzione pesata: feed 40%, notifiche 20%, homepage 15%, restare 25%. Vedi sez.3.
- [x] **Daily report automatico**: `DAILY_REPORT_AUTO_ENABLED=true` + `DAILY_REPORT_HOUR=20`. Report Telegram include: funnel metrics, bot execution, risk score + pending ratio (aggiunto), SLO, selector cache KPI, AB testing, timing slots.
- [x] **Backup automatico**: già implementato nel `run-loop` come sub-task `db_backup` in `loopCommand.ts`. Esegue `backupDatabase()` con WAL checkpoint + file copy (fixato da VACUUM INTO). `BACKUP_RETENTION_DAYS=7` gestito da `scripts/backupDb.ts`.
- [x] **Graceful shutdown**: già implementato. `setupGracefulShutdown()` in `index.ts` gestisce SIGINT/SIGTERM. PM2 `kill_timeout: 10000` confermato in `ecosystem.config.cjs`.

### FASE 5 — Monitoring notturno e weekend
- [x] **Zero attività nel weekend**: già implementato. `isWorkingHour()` in `config/index.ts` ritorna `false` se `weekendPolicyEnabled && (day === 0 || day === 6)`. Lo scheduler ritorna `getWorkingHourIntensity() === 0` → budget zero → nessun job. `WEEKEND_POLICY_ENABLED=true` nel `.env`.
- [x] **Alert notturni**: già implementato. `sendTelegramAlert` in `telemetry/alerts.ts` invia notifiche 24/7 indipendentemente dall'orario. Token Telegram e chat ID configurati nel `.env` (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`). Challenge, 429, quarantine e cookie anomaly generano alert immediati.
- [x] **Security advisor periodico — VERIFICATO FUNZIONANTE**. `securityAdvisor.ts` genera report JSON in `data/security-advisor/security-advisor-{timestamp}.json`. Persiste 7 runtime_flags (`security_advisor_last_run_at`, `_last_status`, `_last_reason`, `_last_report_path`, `_last_findings_count`, `_last_backlog_count`, `_last_error`). Pubblica outbox event `security.advisor.report`. Record audit event. Posture check (`getSecurityAdvisorPosture`) rileva: never executed, stale, last failed, last warn.

### FASE 6 — Manutenzione periodica (settimanale/mensile)
- [x] **Controllo manuale settimanale — strumenti disponibili**. `status` command in `adminCommands.ts` mostra KPI. Dashboard `/api/kpis` ha pending ratio e risk score. `/api/risk/explain` spiega i fattori. `/api/health/deep` verifica DB+queue+proxy. Procedure documentate in GUIDA_ANTI_BAN.md (checklist pre-sessione).
- [x] **Secret rotation mensile — strumenti disponibili**. `secrets-status` command in `adminCommands.ts` (riga ~4 match). `secrets-rotate` con `--apply` flag. `listSecretRotationStatus()` in `repositories/system.ts`. Security advisor verifica automaticamente i segreti scaduti/in warning window.
- [x] **Aggiornamento selettori — automatizzato**. `selectorLearner.ts` esegue nel loop (task #12, once daily). `selector_failures` tracciato in `daily_stats`. `diagnostics --sections selectors` disponibile via CLI. `uiFallback.ts` ha ranking candidati + context cache LRU + dynamic selectors dal DB. Vision fallback tracciato con `visionFallbackUsed` flag.
- [x] **Aggiornamento dipendenze — automatizzato**. Dependabot configurato in `.github/dependabot.yml` (weekly npm minor+patch, monthly Actions). CodeQL SAST nel CI. `npm audit --audit-level=high` nel CI blocca la build per vulnerabilità high/critical.
- [x] **Privacy cleanup automatizzato nel run-loop**. Aggiunto sub-task `privacy_cleanup` in `loopCommand.ts`: esegue `cleanupPrivacyData(config.retentionDays)` ogni 24h, solo il leader. Flag `privacy_cleanup.last_run_at` in runtime_flags. Dynamic import per evitare circular dependency. `onError: 'skip'` → non blocca il ciclo.
