# /goal backlog-operativo — i miglioramenti operativi del backlog 2026-06-09

> **CONFERMATO-APERTO 2026-07-04 — passata `todos-freddi`** (auditor opus, verifica alla fonte; verdetti integrali: `maintenance/2026-07-04-verdetti-todos-freddi.json`)
> Evidenza: Tracker: T1/T1-bis/T2/T3 = [x] DONE+verificati (commit 2cca1d4, 70d3c17; launcher.ts:409-417 viewport adattivo; Ollama default confermato). T4 = [~] leva prodotto. T5 [ ] = unica aperta. T5 = anti-ban: fonte linkedin-bot-backlog-2026-06-09.md:15 «Far funzionare sync col PROXY... proxy MOBILE serializza → browser 85s/nav vs canary 30s; fix = proxy RESIDENTIAL (leva Oxylabs) o bot-side». git log Linkedin (40 commit): goal syncsearch = hardening sync-search (T1-T7, cap durata/volume, block-OS re-lo…
> Azione/causa: NON marcare [x] e NON archiviare: T5 è anti-ban non provato (caution → mai CHIUSO senza evidenza forte). Lasciare T5 come [ ]. Annotare sotto T5 la CAUSA: «Leva utente-only + anti-ban: richiede run live dei 4 workflow col proxy dopo unquarantine + decisione proxy-vs-no-proxy (muro ~85s/nav). L'AI non può eseguirlo (login+account reale); nessuna evidenza di run E2E nel git log Linkedin al 2026-07-04». Aggiungere puntatore alle sedi vive del lever anti-ban proxy: user-actions-pending.md (nota Oxylabs mobile→residential) + cluster LinkedIn prod-readiness.md / workflow-hardening.md. Prossimo passo…


> Binding per `/goal backlog-operativo`. Fonte: gli 8 punti dettati in `linkedin-bot-backlog-2026-06-09.md`.
> Stasera l'audit-orchestratore (#10) è già stato chiuso (10 commit). Qui i punti OPERATIVI residui.
> Gated (browser/launcher/inputBlock): `/antiban-review` + flag PRIMA di ogni Edit.

## END-STATE (misurabile)
I miglioramenti operativi sistemati o verificati con esito chiaro: mouse [WINDOW-BLOCK] ✅, viewport adattivo, ChatGPT/AI realmente attivo (non Ollama fallback), sito dashboard verificato. + i 4 workflow girano E2E (dipende da login+proxy = leva utente parziale).

## Task
- [x] **T1 — Mouse 2° monitor `[WINDOW-BLOCK]`** (commit `2cca1d4`): `_applyClickThrough` passava lo script PS (here-string C# + virgolette) via `-Command` inline+escaping → rompeva l'here-string → click-through non applicato → mouse perso sul 2° monitor. Fix `-EncodedCommand` (base64 UTF-16LE). VERIFICATO A/B reale (EncodedCommand=42, inline=errore) + 3 test, gate 1595. DONE.
- [x] **T1-bis — Mouse "più solido" (richiesta utente 2026-06-09 post-compact)** — ✅ DONE + VERIFICATO E2E (run `b9di2t2u0`: scrape 8 pagine/100 lead, chiusura browser NORMALE, ZERO errori CLIXML, login manuale durante il run sopravvissuto). Commit `70d3c17` (fix) + `5a5abe8` (split SRP <300 righe). FIX APPLICATO commit `70d3c17` (anti-ban, locale, no auto-push). Root cause diagnosi Workflow `w9pjoafcp` (alta conf): click-through OS con buchi → la chrome (X) resta cliccabile. (A) stderr CLIXML via stdio/execFile; (B1) stato multi-PID Set; (B2) timer async ~1s non-bloccante (timing intatto); (B3) canary protetto (`workflowEntryGuards.ts`). `conta-problemi` verde (1595 test). Verifica E2E comportamentale in corso (run `b9di2t2u0`, utente clicca per confermare). Split-plan: estrarre `buildPowerShellScript` (~73 righe C#/Win32) in `windowInputBlockScript.ts` → file <300 (in esecuzione stessa sessione). Residuo: Ctrl+W/Alt+F4 (keyboard-hook, fuori scope "click").
  - DETTAGLIO ORIGINALE: il run E2E `br98xrwq6` ha PROVATO che la pipeline gira (login OK, canary OK, scrape Pagina 1-6 × 25 = 150 lead) ma si è ABORTITA con `WORKFLOW_ERROR: Target page, context or browser has been closed` — il click dell'utente ha chiuso il browser. Due problemi: (a) rumore CLIXML; (b) click chiude la finestra non protetta. il run E2E (`br98xrwq6`) ha PROVATO che la pipeline gira (login OK, canary OK, scrape Pagina 1-6 × 25 = 150 lead) ma si è ABORTITA con `WORKFLOW_ERROR: Target page, context or browser has been closed` — **il click dell'utente ha chiuso il browser e ucciso il workflow**. Due problemi reali emersi dall'output: (a) **rumore CLIXML** `node.exe ... Cannot process the XML from the 'Error' stream` ripetuto → `execSync` inoltra lo stderr della PS interna a node → bot.ps1 prova a deserializzarlo → fix Node `stdio:['ignore','pipe','pipe']`; (b) **il click chiude il browser** → click-through con stato SINGLETON (`_lastPid` unico) + copertura finestre incompleta → una finestra resta non protetta. Diagnosi parallela in corso (Workflow `w9pjoafcp`: ciclo canary, overlay DOM, SOTA input-block). Fix INLINE gated (antiban-review). **Conferma direzione**: `--no-proxy` end-to-end OK, il bug residuo è SOLO la solidità del blocco input.
- [x] **T2 — Viewport adattivo** ✅ GIÀ RISOLTO (verificato alla fonte 2026-06-09, quasi-falso come il backlog): per Camoufox (engine in uso) la finestra è GIÀ adattiva allo schermo reale → `launcher.ts:409-417` legge `[System.Windows.Forms.Screen]::PrimaryScreen.WorkingArea`; fallback `[1366,768]` se il read fallisce; `1920×1080` SOLO headless; non-headless `viewport=null` + `window: cfxWindow` = schermo reale. Il "1463×866 fisso" del backlog è uno stato superato. Anti-ban: windowSize = schermo reale è PIÙ coerente col fingerprint (un umano usa la dimensione del suo monitor). NESSUN fix da fare.
- [x] **T3 — Cervello AI / ChatGPT collegato davvero?** ✅ VERIFICATO (2026-06-09): NON è ChatGPT. Default = **Ollama locale** (`OPENAI_BASE_URL=http://127.0.0.1:11434/v1`, `AI_MODEL=llama3.1:8b`, `AI_ALLOW_REMOTE_ENDPOINT=false`). Circuit `cb::openai.chat` = CLOSED (non è quello il blocco). `requestOpenAIText` throwa "endpoint remoto bloccato" se non-locale e allowRemote=false. NON è un bug di codice → **CONFIG (leva utente)**: per ChatGPT reale servono nel `.env`: `OPENAI_BASE_URL=https://api.openai.com/v1` + `OPENAI_API_KEY=sk-...` + `AI_ALLOW_REMOTE_ENDPOINT=true` + `AI_MODEL=gpt-4o`/gpt-5. (Collegato al piano gpt-5.5 in `~/.claude/plans/twinkling-sauteeing-pinwheel.md`.)
- [~] **T4 — Sito/dashboard di controllo** (CONTENUTO verificato 2026-06-09): `dashboard/pages/index.js` è un **TodoDashboard** — gestisce TODO (title/status/priority/agent) via Supabase Realtime. **NON è un pannello di controllo del bot LinkedIn** (niente lead/invii/pending ratio/stato run/anti-ban). Per "controllare il sistema" serve un dashboard DIVERSO: o espandere questo con le metriche bot (riusa `/lead-report`, riskEngine, daily_stats), o usare la pagina "OS" della presentazione (già ricca ma statica). **Decisione prodotto = leva utente** (espandere vs sostituire). La parte AI (verifica del contenuto) è fatta.
- [ ] **T5 (leva utente) — 4 workflow E2E col proxy**: dopo `unquarantine` (login già ok: li_at presente) → run + osservare + misurare timeout canary col proxy (85s muro). Decisione anti-ban: proxy vs --no-proxy.

## Note
- Login OK (li_at presente 2027). Quarantena ANCORA attiva (`unquarantine` prima degli outreach).
- Proxy: mobile è il migliore anti-ban (SOTA, NO residential); muro = 85s/nav (timeout canary).
- Punti NON-operativi del backlog (tracciati altrove): presentazione ✅, sync intelligente ✅ (640be08), ferry gpt-5.5 (attesa diff), web-analysis (attesa spiegazione utente).
