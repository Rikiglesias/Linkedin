# LinkedIn hardening 2026 — backlog azioni da `docs/research/LINKEDIN_STUDY_2026.md`

> Generato 2026-06-08 dallo studio `w3s73o157`. Dettaglio + fonti + evidenza file:riga nel doc.
> FASE: adattamento workflow (post-studio). Ogni item che tocca anti-ban/fingerprint/scheduler →
> `/antiban-review` SAFE + quality gate, applicato INLINE (mai delegato). GAP-1/2/3 = anti-ban → conferma utente prima.
> Ordine = severity. Si parte dal #1.
> **GAP-1/2/3 APPROVATI dall'utente 2026-06-08 ("ok gap")** → procedere #2 (cap durata-sessione, sul run-loop loopCommand/jobRunner, NON scheduler-budget) → #3 (JA3↔UA kill-switch) → #8 (timezone↔proxy), ciascuno con /antiban-review SICURO + flag PER EDIT. #2 è il più architetturale (spazia scheduler+run-loop) e timing-critical.

## CRITICAL
- [x] **1. Site-check gate su segnali inaffidabili** → `c0d7237`. `isSiteSignalsReliable()`: se messageButton+connectButton+pendingIndicator TUTTI assenti → pagina inaffidabile (selettori stale o non caricata) → il loop SALTA il lead (no classify/auto-fix/review) + conta `suspectedDrift` + `logWarn site_check_selector_drift_suspected` (non-throwing) con driftRatio. Strettamente più sicuro (profilo high-connection con Connect in overflow oggi → REVIEW errato; ora → skip tracciato). +test in unit.vitest. conta-problemi exit 0 (1560 test). Anti-ban: nessuna azione browser nuova.

## HIGH
- [x] **2. Cap durata-sessione** → `53b40bb` (antiban SICURO). VERIFICA zero-M: il cap NON mancava — esiste in jobRunner (`sessionMaxMs=randomInt(min,max)` + wind-down + rotazione con re-login, righe 399/403/1162). La sintesi guardava solo scheduler.ts=budget. Delta reale: range default 35-70min, max 70 sopra il limite scheduler-rules.md "5-45" → allineato a **25-45min** in domains.ts. conta-problemi exit 0 (1561). 
- [x] **3. Coerenza UA↔TLS hard-constraint** → `05ef849` (antiban SICURO). VERIFICA zero-M: la coerenza c'era già (`filterTlsCoherentPool` tiene solo UA coerenti con l'engine) ma applicata **solo al pool locale**; il path **cloud** la bypassava = gap reale. Fix: estratto `isUaTlsCoherentWithEngine()`, applicato anche al pool cloud (soft-fallback, no kill-switch brusco). CycleTLS-safe. conta-problemi exit 0 (1561).
- [x] **4. Vision/fallback + drift sul click Connect invito** → `5b95634` (antiban SICURO). `clickConnectOnProfile`: quando primario+overflow falliscono → `clickWithFallback(SELECTORS.connectButtonPrimary, 'connect-button', {verify: modal-invito})` = self-healing engine (chain drift-aware + Vision/LLaVA) già in prod; su fallimento totale `trackSelectorFailure` (drift visibile, no SKIP silenzioso). `verify` esige il modal → Vision non clicca Follow/Message. conta-problemi exit 0 (1560).
- [x] **5. Overflow menu rename/lazy-aware** → `c619b5d` (antiban SICURO). selectors.ts moreActionsButton +"Resources"/"Risorse" (aria-label+has-text+XPath); inviteWorker: `humanDelay(700,1300)` fisso → `humanDelay(350,700)` + `waitForSelector(connectInMoreMenu, 3000)` non-throwing per il lazy-render. Varianza preservata. (Residuo: "Connect inesistente come stato valido" confluisce in #4 Vision/drift.) conta-problemi exit 0 (1560).
- [x] **6. Detection CUL** → `80ec363` (antiban SICURO). `detectCommercialUseLimit(page)` (body-text regex EN+IT) in openProfileViaSearchResults: se CUL → logWarn WHAT/WHY/DO + `pauseAutomation('COMMERCIAL_USE_LIMIT', …, null)` (pausa indefinita, resume manuale) + return false → stop hammering. madge ok (no cycle). +1 test. conta-problemi exit 0 (1561). (Residuo opz: wiring del detect anche in site-check goto + budget-ricerche proattivo < ~250/mese.)

## MEDIUM
- [x] **7. WebRTC-leak** — ✅ **VERIFICATO A RUNTIME (2026-06-08)**: `node dist/scripts/webrtcLeakCheck.js --allow-direct --headless` → `RTCPeerConnection` killato (undefined), 0 ICE candidate, **✅ PASS** nella sessione camoufox reale. egress=proxy NON testato (proxy down, vedi blocker sotto) ma MOOT: RTC totalmente killato = nessun canale da leakare. LATO-CODICE DONE+TESTATO (verificato zero-M), conferma runtime FATTA. Protezione a 4 layer: `camoufoxBlockWebrtc` default **true** (domains.ts:397) + RTCPeerConnection/webkit/moz killati (stealthScripts:79-85) + `--disable-webrtc` chromium (launcher:324) + `block_webrtc` camoufox (launcher:465). Già coperto da test (`stealth.vitest.ts:42` toContain RTCPeerConnection + 156-157 assert undefined). RESTA SOLO: conferma runtime su staging (no STUN-leak IP reale) + check versione camoufox v146-beta. Se leak → fork mantenuta (CloverLabs) o patch `proxy_only_if_behind_proxy`. **TURNKEY (commit `6c64b4b`)**: `npm run webrtc:leak-check` automatizza il test — lancia il browser col proxy gestito (fail-closed), legge l'egress IP (api.ipify.org) e verifica in pagina che RTCPeerConnection sia killato, exit 0=PASS/1=FAIL; `-- --headless` per CI, `-- --allow-direct` per testare solo il kill RTC senza proxy. Resta solo lanciarlo tu su staging con proxy reale. **ESITO sessione 2026-06-08**: l'utente ha incollato un test browserleaks (RTCPeerConnection=True + Public IP WebRTC raccolto = 195.32.113.71, No Local IP Leak) e ha lanciato `/goal clear`. **Lettura (zero-M)**: RTC funzionante + candidate raccolti ⟹ stealth NON iniettato ⟹ era un **browser normale**, NON la sessione camoufox del bot (le 2 barriere block_webrtc + kill JS non possono fallire entrambe). 195.32.113.71 ≈ IP reale IT, non l'egress proxy. Quindi quel test NON valida la protezione del bot (mostra il baseline-minaccia). **Verifica definitiva ancora da fare nella sessione vera**: `npm run webrtc:leak-check` → atteso `RTCPeerConnection ... killato ✓` + egress = IP proxy + `✅ PASS`. #7 resta [~] finché non si vede quel PASS dallo script (goal formale chiuso dall'utente). **Run 1 (fallito, fix `8240210`)**: `npm run webrtc:leak-check` crashava con `impit couldn't load native bindings` — ROOT CAUSE: lo script era cablato `ts-node` (l'unico ts-node che apre camoufox); ts-node intercetta `require.extensions['.js']` e rompe il loader nativo impit (transitivo da camoufox-js). Provato: `require('impit')` sotto node puro OK, sotto ts-node throwa. Il bot lancia il browser SEMPRE compilato (create-profile=`node dist/index.js`, ramp-up/db:backup=`node dist/scripts/X.js`). Fix: npm script → `npm run build:backend && node dist/scripts/webrtcLeakCheck.js`. Verificato: build exit 0 + modulo compilato carica sotto node senza crash. **Re-run utente ora possibile.** (Follow-up igiene separato: `camoufox-js@0.9.1 invalid` vs `^0.9.3` richiesto — install stale; `npm install` per riconciliare ma tocca package-lock condiviso col peer Codex → coordinare.)
- [x] **8. timezone↔proxy** → `a6c1560` (antiban SICURO). VERIFICA zero-M: GIÀ GESTITO per **camoufox** (prod): launcher passa `geoip`=proxy → camoufox auto-deriva tz/locale/geo dal geoip = segue il proxy. Residuo narrow = solo path **chromium** (:348, non-prod) con config.timezone; per IT-only (proxy IT) Europe/Rome è coerente. Aggiunto commento che documenta lo stato + guida chromium multi-geo (TIMEZONE env o M18). Wiring M18→chromium = follow-up low. conta-problemi exit 0 (1561).
- [x] **9. Consolidare `Save in list` vs `Save to list`** → `b26a2cf` (antiban SICURO). Varianti "Save in list"/"Salva in lista" assorbite nel centrale `SALESNAV_SAVE_TO_LIST_SELECTOR`; `listActions.SAVE_TO_LIST_BUTTON_SELECTOR` ora alias del centrale (caller invariati). +test anti-divergenza. conta-problemi exit 0 (1561). (Residuo opz: `listScraper` SHOW_MORE locale vs centrale — minore.)
- [~] **10. Copertura locale FR/DE/ES** — RISOLTO con decisione di scope (DECIDE-DON'T-ASK, default sensato): **"solo IT/EN"**. Il bot opera IT/EN (selettori, config Europe/Rome, locale it-IT); FR/DE/ES = espansione-mercati speculativa non indicata → non si costruisce lavoro per un bisogno assente. NON indovinare i testi foreign (mezzo-lavoro). **Refactor strutturale tracciato per espansione** (runtime UI-lang-detection da `document.documentElement.lang` + `getByRole(name: regex)` + mappa testi per locale) → `improvements-proposed.md`. Reversibile: se l'utente pianifica espansione → "refactor" e si fa. L7-LI.5 resta soddisfatta per lo scope reale (IT/EN); i flussi message/followup/bulksave degradano comunque al Vision su lingue impreviste.

## LOW
- [x] **11. Guard profilo `aggressive`** (parte SAFE) → `ede257a`. Guard-comment in schema.ts: opt-in solo SSI>65/acc>40%/età>6m, pending 0.7 sopra zona-rischio 2026, no account nuovi. Soglia 0.7 NON cambiata (intenzionale per opt-in; abbassare il default = leva utente, tracciata). conta-problemi exit 0 (1561). (Residuo leva-utente: decidere se abbassare il default aggressive pending 0.7→0.6.)
- [x] **12. Drift-tracking scraper read-only** → `4487132` (antiban SICURO). `scrapeLinkedinProfile`: counter scrape consecutivi tutti-vuoti (headline+title+company) → a soglia 5 `logWarn scraper_drift_suspected` + reset; logica pura `nextScraperDriftState` (+4 test). conta-problemi exit 0 (1561). (Residuo opz: stesso pattern su `ssiScraper`.)

## NUOVO BLOCKER scoperto (2026-06-08, runtime, dal test #7) — proxy-auth 407 nel geoip
- **`camoufox publicIP()` fallisce con 407 ProxyAuthRequired** durante il launch quando geoip+proxy attivi. `launchOptions` (camoufox-js utils.js:388) chiama `publicIP(proxyUrl.href)` per scoprire l'IP di uscita reale; impit non autentica sul proxy Oxylabs (password con caratteri speciali — il nostro launcher.ts:450 già logga `passwordHasSpecialChars`). `camoufoxGeoip` default TRUE (domains.ts:395) → **path attivo ovunque con proxy, non solo Windows dev → potenzialmente prod-affecting**.
- **Sospetto primario**: camoufox-js **0.9.1 installato (stale)** vs `^0.9.3` richiesto; 0.9.x ha fix su proxy/geoip. **Fix candidato**: `npm install` per riconciliare a 0.9.3 (tocca package-lock condiviso col peer Codex → coordinare) + re-test. Fallback se non basta: geoip manuale (CAMOUFOX_GEOIP=false + tz/locale IT espliciti) via antiban-review.
- **#7 sbloccato intanto con `--allow-direct`** (`fdc862d`): bypassa il proxy → verifica SOLO il kill RTC (sufficiente per il cuore della protezione). `npm run webrtc:leak-check -- --allow-direct`.
- **DA UTENTE**: il bot parte OK in prod/staging ADESSO con queste credenziali Oxylabs? (se sì → prod sano, fix = riconciliare dev; se no/mai testato → bug latente ovunque). OK a `npm install` (lockfile condiviso)?
- **✅ RISOLTO (2026-06-08): NON è un bug.** L'utente ha rivelato: sub-user `linkedinproxy_1skgm`, **1 GB di traffico gratis Oxylabs ESAURITO**. Quota a zero → Oxylabs blocca il sub-user → 407 (impit) + NS_ERROR_PROXY_CONNECTION_REFUSED (Firefox) su TUTTO. Credenziali valide, codice proxy/camoufox/impit a posto (indagine ha trovato zero difetti di codice). **Fix = ricaricare traffico / upgrade piano Oxylabs = leva utente.** camoufox-js upgrade 0.9.3 era inutile → lock revertato (tree pulito), node_modules locale resta 0.9.3 (si riconcilierà al prossimo `npm install`). Bot fa fail-closed su IP diretto (AB-24) → non gira finché proxy a zero (corretto: meglio fermo che de-anonimizzato).

## Note
- Verifica live selettori sul LinkedIn reale = leva utente (login + account veri).
- I primi 6 (critical+high) sono il nucleo "workflow perfetti + non farci beccare".
