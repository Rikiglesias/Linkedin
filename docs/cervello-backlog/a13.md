# /goal a13 — Split A13: file anti-ban >300 righe (regression-safe)

> Binding GOAL_TASK_BINDING. Creato 2026-06-13. Progetto: `C:\Users\albie\Desktop\Programmi\Linkedin`.
> Origine: residuo audit-bot A13. Utente "vai A13" (2026-06-13). **Eseguire in CHAT FRESCA** (refactor stealth-core: contesto pulito = priorità anti-ban, qualità-o-niente).

## End-state (misurabile)
I file anti-ban >300 righe splittati in moduli SRP <300, **REGRESSION-SAFE** (zero-Q): comportamento IDENTICO (copia letterale delle formule timing/varianza, zero cambio logica), `madge --circular`=0, test verdi, `/antiban-review` SICURO dopo OGNI file. Facade re-export → i caller NON cambiano import (backward-compat). `conta-problemi` exit 0.

## Ordine (per valore/rischio — decido io, zero-C.10)
1. **humanBehavior.ts** (1464 righe, stealth-core) — outlier, Workflow "basso rischio, responsabilità stratificate". PRIMO.
2. **bulkSaveOrchestrator.ts** (1839, salesnav) — il più grande/complesso (loop 943 righe). SECONDO.
3. **proxyManager.ts** (933) / **launcher.ts** (932) — **BORDERLINE** (zero-I: 3× soglia, refactor anti-ban funzionante per igiene). Solo se i 2 outlier vanno lisci e il valore lo giustifica. Design dettagliato già in output Workflow.

## Design split humanBehavior (verificato alla fonte: 29 export)
Famiglie SRP → moduli `src/browser/human/`:
- `mouseState.ts` — **BASE/stato condiviso** (initializeMouseState, releaseMouseConfinement + lo state mouse/confinement). Estrarre PRIMA (gli altri lo importano).
- `inputBlock.ts` — ensureInputBlock, pauseInputBlockForMove/resume, pauseInputBlock/resume, blockUserInput (285-516).
- `cursorOverlay.ts` — ensureVisualCursorOverlay, enableVisualCursorOverlay, removeAllOverlays, pulseVisualCursorOverlay (UI debug).
- `humanDelay.ts` — **TIMING-CRITICO**: humanDelay, ensureViewportDwell, interJobDelay, computeProfileDwellTime, contextualReadingPause. Copia LETTERALE delle formule (log-normale).
- `mouseMovement.ts` — **TIMING-CRITICO**: humanMouseMove, humanMouseMoveToCoords, randomMouseMove (Bézier/Fitts). Copia letterale.
- `touchGestures.ts` — humanTap, humanSwipe.
- `humanTyping.ts` — **TIMING-CRITICO**: humanType (typo/cadenza). Copia letterale.
- `readingSimulation.ts` — simulateTabSwitch, simulateHumanReading.
- `decoyActions.ts` — performDecoyBurst, performDecoyAction.
- `awaitManualLogin` → valutare se in auth o resta nel facade.
- `humanBehavior.ts` → **facade** re-export di tutto (backward-compat: ~30 caller non cambiano import).

## Metodo regression-safe (zero-Q, leaf-first)
1. **Baseline**: `madge --circular src/browser/` + lista test esistenti che toccano humanBehavior (grep) + `conta-problemi` verde.
2. **Estrazione leaf-first**: mouseState (base) → famiglie foglia → famiglie che le usano → facade. Ogni modulo: copia LETTERALE (zero cambio formule), import corretti, poi `madge` + test + `/antiban-review` PRIMA del chunk successivo.
3. **Stato condiviso**: mouseState/inputBlock estratti in moduli con ownership chiara; gli altri importano (NON duplicano lo state — un solo modulo possiede ogni Map/variabile).
4. **Anti-ban GUARD**: le formule timing (delay log-normale, Bézier, typo cadence) NON vanno mai riscritte/arrotondate — solo spostate verbatim. Un drift numerico = fingerprint mutation = detection.
5. File gated (`src/browser/**`) → flag `antiban-approved.txt` PRIMA di ogni edit + `/antiban-review`.

## Riferimenti (design dettagliato già prodotto)
- Output Workflow A13 (mapping completo 4 file + regressionSafetyPlan + antibanRisks per-file): `tasks/wub0irtla.output` (sessione 2026-06-13). Rileggere per proxyManager/launcher (ordine estrazione preciso) e bulkSaveOrchestrator.
- Mapping sintetico in `docs/LINKEDIN_IMPLEMENTATION_LIST.md` + `~/todos/audit-bot.md`.

## Antiban-review (concept split)
✅ SICURO **a condizione di copia letterale esatta** delle formule timing + preservazione stato condiviso. Le 6 domande: browser-behavior NO (refactor puro), timing NO-se-verbatim, fingerprint NO, nuova-azione NO, volumi NO, behavioral-pattern preservato. Vigilanza MASSIMA sul punto 2 (timing): ogni chunk → `/antiban-review` dedicato sul diff.

## Stato esecuzione

### File 1 — humanBehavior.ts ✅ COMPLETO (2026-06-13, chat fresca timing)
Split leaf-first DAG, `src/browser/human/` — 10 moduli tutti <300, madge=0, conta-problemi exit 0 (181f/1783t = baseline), costanti timing VERBATIM zero-drift:
- [x] **mouseState.ts** (`cb5b057`) 61r — stato posizione mouse. NON-timing.
- [x] **overlayIds.ts** (`ba1447c`) 18r — ID DOM overlay. NON-timing.
- [x] **cursorOverlay.ts** (`b52d3cc`) 170r — VisualCursorOverlay. NON-timing (DOM).
- [x] **inputBlock.ts** (`c53624d`) 243r — input-block overlay. NON-timing (DOM/click).
- [x] **touchGestures.ts** (`0db75c9`) 99r — humanTap, humanSwipe.
- [x] **humanDelay.ts** (`c936b99`) 84r — humanDelay, ensureViewportDwell, contextualReadingPause. ⚠️ TIMING-CORE (log-normale asimmetrica).
- [x] **mouseMovement.ts** (`bd6aec9`) 186r — withMouseTimeout, humanMouseMove(ToCoords), randomMouseMove. ⚠️ TIMING-CORE (Bézier/Fitts).
- [x] **readingSimulation.ts** (`f670307`) 231r — wheelWithMomentum, simulateTabSwitch, simulateHumanReading, **computeProfileDwellTime** (spostata qui, era in humanDelay nel design: evita ciclo orchestratore→primitive).
- [x] **humanTyping.ts** (`b043f66`) 142r — humanType. ⚠️ TIMING-CORE (keystroke floor 55/80ms).
- [x] **decoyActions.ts** (`3f0bbab`) 234r — performDecoyBurst/Action, runDecoyStep, shuffle, DECOY_SEARCH_TERMS.
- [x] **humanBehavior.ts → facade** (`c2606fb`) **195r** — re-export di tutto + 2 orchestratori di sessione TENUTI qui: **awaitManualLogin** (login) + **interJobDelay** (pacing). `sessionPacing.ts` NON creato (zero-I: no modulo single-use; facade già 195<300, interJobDelay è orchestratore cohesivo con l'entry).

humanBehavior: 1464 → **195** righe. DAG senza cicli, zero dynamic-import nuovi (solo i pre-esistenti overlayBridge/auth). Facade re-export → ~30 caller invariati.

### File 2 — bulkSaveOrchestrator.ts (1839, salesnav) — ANALIZZATO, esecuzione fresh chat
> `tasks/wub0irtla.output` (vecchio design) NON esiste più. Mappa RICOSTRUITA via subagent code-explorer (2026-06-13), salvata QUI.

**DECISIONE UTENTE (2026-06-13): Opzione A** — solo helper safe, orchestratore resta >300 = eccezione giustificata L1.6 (anti-ban). NON decomporre il loop. NON toccare il funzionante oltre le estrazioni verbatim. `bulkSaveHelpers.ts` già ~350r → step 1-2 valutare se creare nuovo modulo invece di gonfiarlo.

**ESECUZIONE Opzione A (2026-06-13) — moduli puliti <300 ESTRATTI:**
- [x] **bulkSaveNavigation.ts** (282r, `d245245`) — waitForManualLogin + navigateToSavedSearches + SEARCHES_URL (re-export) + VIEW_SAVED_SEARCH_SELECTOR.
- [x] **bulkSaveSearchDiscovery.ts** (197r, `3b4b51d`) — waitForSearchResultsReady, normalizeSearchName, extractSavedSearches (re-export), ensureNoChallenge, verifyVisionSurface, clickSavedSearchView. Importa VIEW_SAVED_SEARCH_SELECTOR da navigation (DAG).
- orchestrator **1839→1409** (−430r). madge=0, conta-problemi exit 0 (181f/1783t = baseline). Antiban SICURO.
- **NON estratti (decisione zero-I):** `preSyncListToDb` (287r) + `processSearchPage` (40r ALTO rischio) → estrarli creerebbe altri file >300 senza win <300; restano nell'orchestratore (giustificato). `buildInitialReport`(24)/`resolveSearchFilter`(~100, dentro il loop) = micro-estrazioni OPZIONALI residue (basso valore). **File 2 Opzione A = COMPLETO per la parte safe.**

**FINDING CRITICO (decisione utente, zero-G):** `runSalesNavBulkSave` (896-1839, 943r) NON è portabile <300 regression-safe. Il for-loop interno pagine (1336-1747, ~411r) ha 8 var di stato mutabili intrecciate con logica anti-ban (early-stop duplicati, health-check AI ogni 8 pag, challenge-detect, backoff esponenziale) → estrarlo = MOLTO ALTO rischio regressione. Anche estraendo TUTTI gli helper, il file resta ~1080r (l'orchestratore è 943). **Opzioni:** (A) estrai solo helper safe → file ~1080, orchestratore resta >300 = eccezione GIUSTIFICATA (L1.6, anti-ban) — RACCOMANDATO; (B) decomponi anche il loop in micro-funzioni stateless (mergeScrollProfiles/checkEarlyStop/handleChallengeInLoop) senza toccare l'ordine await → riduce ~100r, ALTO rischio, /antiban-review rinforzato.

**Stato module-level:** `SEARCHES_URL` (export, 64), `_loadHandlerRegistered` WeakSet (68, resta nell'orchestratore col `page.on('load')` 939-946), `VIEW_SAVED_SEARCH_SELECTOR` (70-76, segue navigateToSavedSearches).

**Estrazioni leaf-first (verbatim, NON toccare logica/timing):**
1. `normalizeSearchName`(173) + `buildInitialReport`(570) → `bulkSaveHelpers.ts` — BASSO (funzioni pure)
2. `waitForSearchResultsReady`(136) + `ensureNoChallenge`(421) → `bulkSaveHelpers.ts` — BASSO (DOM/check)
3. `extractSavedSearches`(387, export) + `resolveSearchFilter` (estrai match-logic Fase 2, ~1032-1132, funzione pura su array) → NUOVO `bulkSaveSearchDiscovery.ts` — BASSO
4. `verifyVisionSurface`(438) + `clickSavedSearchView`(480) → `bulkSaveSearchDiscovery.ts` — MEDIO (esportare `VIEW_SAVED_SEARCH_SELECTOR` o passarla param; clickSavedSearchView usa waitForSearchResultsReady)
5. `preSyncListToDb`(608, 287r) → NUOVO `bulkSavePreSync.ts` — MEDIO-ALTO (verbatim, 287r timing/nav, zero modifica logica)
6. `navigateToSavedSearches`(183, 203r) + `VIEW_SAVED_SEARCH_SELECTOR` + `SEARCHES_URL` → NUOVO `bulkSaveNavigation.ts` — ALTO (verbatim, nav 3-step humanDelay)
7. `waitForManualLogin`(83) → `bulkSaveNavigation.ts` o helpers — ALTO (session-recovery overlay, verbatim)
8. `processSearchPage`(529) → valutare `bulkSavePageActions.ts` (già esiste) — ALTO (retry loop save-to-list)

**NON estrarre:** for-loop pagine 1336-1747 (MOLTO ALTO). Moduli esistenti NON toccare: bulkSaveTypes/State/Pagination/PageActions/Helpers.
**Metodo:** identico a File 1 — flag antiban-approved per ogni Edit gated (`src/salesnav/**` gated), Write verbatim + fix path import, facade re-export, tsc+madge per chunk, /antiban-review, commit. Caller esterni invariati via re-export.

### File 3-4 — proxyManager.ts (933) / launcher.ts (932) — SKIP (decisione utente 2026-06-13)
zero-I: 3× soglia ma anti-ban CORE FUNZIONANTE (proxy stickiness, browser launch/fingerprint). **Decisione utente: SKIP** — refactor solo-igiene su superficie sensibilissima = rischio > valore. Resta opzionale futuro (se mai: chat fresca + /antiban-review rinforzato + analisi preventiva).

## 🟢 STATO GOAL a13: CHIUSO (2026-06-13, decisione utente)
- **File 1 humanBehavior**: 1464→195r, 10 moduli <300 — DONE
- **File 2 bulkSaveOrchestrator**: 1839→1409r, Opzione A (navigation 282 + discovery 197); orchestratore >300 giustificato (loop anti-ban non-splittabile) — DONE
- **File 3-4**: SKIP per decisione utente (zero-I)
- Regression-safe: madge=0, conta-problemi exit 0 (181f/1783t = baseline), antiban SICURO. Pushato su origin/refactor/adk-split fino a `f786562`.
