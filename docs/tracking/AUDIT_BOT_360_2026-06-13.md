# Audit 360° del bot LinkedIn — 2026-06-13

> Goal `audit-bot` (binding `~/todos/audit-bot.md`). Branch `refactor/adk-split`.
> Metodo: read-only alla fonte viva (file:riga, non a memoria); 4 aree esplorate in parallelo (explorer agent),
> A1 analizzata inline. Fix BOUNDED a basso rischio → inline (antiban-review + test + commit); il resto → backlog.
> Legenda priorità: **CRIT** (anti-ban / data-integrity / GDPR) > **HIGH** (robustezza) > **MED** (UX/metriche) > **IGI** (igiene).

## Stato aree

| Area | Stato | Esito sintetico |
|---|---|---|
| A1 Terminale/CLI | ✅ auditata + **1 fix applicato** | Bug anti-ban `askConfirmation` default → FIXATO. 4 file CLI >300 righe (igiene). |
| A2 Workflow sync-list/search | ✅ auditata | Anti-ban navigazione SOLIDO, idempotenza OK. Fragile su fallback parziali (cloud/enrichment) + checkpoint race. |
| A3 Orchestratore/jobRunner | ✅ auditata | Lock/handoff robusti, MA 3 buchi critici: NPE su session-rotate, browser-leak, job stale post-block. |
| A4 Secondo cervello/AI | ✅ auditata | Zero-PII OK, nessun residuo "fable", routing Opus+Haiku corretto. Feedback loop incompleto (3 GAP). |
| A5 Collegamento DB | ✅ auditata | SQLite SSOT atomico OK. Fragilità cloud: schema drift, colonne divergenti, GDPR edge-case, no timeout. |
| A6 Risk/antiban guards | ⏳ pending | — |
| A7 Scheduler/timing | ⏳ pending | — |
| A8 Proxy/stealth/fingerprint | ⏳ pending | — |
| A9 Workflow n8n | ⏳ pending | — |
| A10 Dashboard/API/frontend | ⏳ pending | — |
| A11 Telemetria/observability | ⏳ pending | — |
| A12 Lifecycle/multi-account | ⏳ pending | — |
| A13 Igiene architetturale | ⏳ pending (parziale: file >300 raccolti sotto) | — |

---

## A1 — Terminale interattivo / CLI UX

**Mappa**: `bot.ps1` (42, launcher + rebuild incrementale) → `src/index.ts` (752, dispatcher switch) →
`src/cli/{cliParser.ts(95), stdinHelper.ts(125), commandHelp.ts(246)}` + `src/cli/commands/{preflightEnv.ts(188),
replCommand.ts(52), adminCommands.ts(1170), loopCommand.ts(1167), salesNavCommands.ts(726), utilCommands.ts(875),
workflowCommands.ts(83)}`. Pre-flight interattivo = `src/workflows/preflight.ts` (L1–L6 + AI advisor).

**Cosa funziona** (con evidenza):
- `parseIntStrict` (cliParser.ts:53-64): regex `^-?\d+$` rifiuta "12abc" — no troncamento silenzioso.
- `readLineFromStdin` (stdinHelper.ts:15-52): cleanup listener nominati (no MaxListeners leak) — L3 ok.
- Pre-flight con timeout esplicito 5s su fetch (ollama preflightEnv.ts:65-70, anthropic :89-99) + AbortController.
- Anthropic check usa `GET /v1/models` (preflightEnv.ts:91) — valida key senza consumare token.
- Doctor gate obbligatorio con whitelist safeCommands (index.ts:296-298); `preflight-env` read-only escluso correttamente.

**Problemi**:
| # | Sev | File:riga | Problema |
|---|---|---|---|
| A1-1 | **CRIT** | stdinHelper.ts:57-60 | `askConfirmation` ritornava `true` su INVIO-a-vuoto **sempre** (default YES hardcoded), ignorando il prompt. 5 call-site `[y/N]` (default NO atteso) venivano forzati premendo solo INVIO o in non-TTY: `preflight.ts:185` (override ABORT AI), `antiBanChecklist.ts:55` (warmup insufficiente), `antiBanChecklist.ts:76` (pending ratio oltre STOP), `loopCommand.ts:1062` (budget), `adminCommands.ts:530` (cancellazione **IRREVERSIBILE** dati). → **FIXATO** (vedi sotto). |
| A1-2 | MED | stdinHelper.ts:65-79 | `askNumber` valida `>= 0` ma **senza upper bound**: un typo "9999" su un volume passerebbe (mitigato dal cap del risk engine a valle, ma manca defense-in-depth). |
| A1-3 | IGI | adminCommands.ts(1170), loopCommand.ts(1167), utilCommands.ts(875), salesNavCommands.ts(726), index.ts(752) | 5 file CLI >300 righe; `loopCommand` è anti-ban-touch (timing). Candidati split. |

**Fix applicato (A1-1)** — fix di CLASSE:
- `stdinHelper.ts`: estratta `parseConfirmationAnswer(answer, defaultValue)` (pura, testabile) + `askConfirmation(prompt, defaultValue=true)`. Empty → `defaultValue`; `y/s` → true; resto → false. Retrocompatibile: i `[Y/n]` (default true) restano identici.
- 5 call-site `[y/N]` aggiornati a `defaultValue=false` (preflight.ts:186, antiBanChecklist.ts:56+78, loopCommand.ts:1062, adminCommands.ts:531).
- Test nuovo `src/tests/stdinHelper.vitest.ts` (5 casi: empty/default, affermative, negative/ambiguo, spazi).
- **Antiban-review: SICURO/migliorativo** — i gate pending-ratio/warmup/override-AI non sono più forzati di default o in non-TTY.

**Backlog A1**: A1-2 (cap su askNumber per volumi outreach), A1-3 (split file CLI).

---

## A2 — Workflow sync-list / sync-search

**Mappa**: `syncListService.ts:127` / `syncSearchService.ts:95` → `salesNavigatorSync.ts:1101` (engine) →
`listScraper.ts:650` (scrape) / `bulkSaveOrchestrator.ts` → `leadsCore.ts:375` (upsert) → `supabaseDataClient.ts` (cloud).

**Funziona**: scroll log-normale + hover random + `humanDelay` (listScraper.ts:110,244,657) → navigazione umana SOLIDA;
upsert idempotente INSERT/UPDATE-merge in transazione (leadsCore.ts:375-427); challenge detection non-bloccante
(listScraper.ts:693-695); checkpoint non avanza se `scrapeDegraded` (salesNavigatorSync.ts:1165).

**Problemi**:
| # | Sev | File:riga | Problema |
|---|---|---|---|
| A2-1 | **CRIT** | salesNavigatorSync.ts:604-608 | Outbox push fallito → solo `console.warn`, niente alert/retry → lead cloud-sync persi silenziosamente. |
| A2-2 | HIGH | salesNavigatorSync.ts:323-530 | Enrichment loop senza retry/backoff per-lead: se Apollo down, 300 lead falliscono in cascata cieca. |
| A2-3 | HIGH | salesNavigatorSync.ts:1165 + `.catch(()=>null)` | Checkpoint write per-lista non atomico: network-interrupt → lista ri-tentata (idempotente, ma inefficiente). |
| A2-4 | MED | listScraper.ts:56 | Selector chain debole vs `bulkSaveOrchestrator.ts:146-156` (che ha fallback): DOM change → subito `scrapeDegraded`. |
| A2-5 | MED | salesNavigatorSync.ts:572-581 | `cloudErrors` = conteggio batch, non lead → report ambiguo ("1 errore" = 0/300 o 280/300?). |
| A2-6 | IGI | bulkSaveOrchestrator.ts(1839!), salesNavigatorSync.ts(1198) | File enormi, split candidato. |

---

## A3 — Orchestratore / jobRunner

**Mappa**: `orchestrator.ts:242` (`runWorkflow`) → `scheduler.ts:398` (`scheduleJobs`) → `jobRunner.ts:184`
(`runQueuedJobsForAccount`); lock `repositories/jobs.ts:61-116`; quarantena `incidentManager.ts:36`.

**Funziona**: handoff canary→jobRunner con cleanup finally (orchestrator.ts:250-266, jobRunner.ts:1395-1456);
lock DB `FOR UPDATE SKIP LOCKED` (jobs.ts:61-116); challenge mutex per-account (jobRunner.ts:182,828-851);
circuit breaker lista 4h (jobRunner.ts:985-1014); risk/compliance guard pre-run (orchestrator.ts:332-431).

**Problemi** (⚠ findings explorer — DA VERIFICARE alla fonte prima di fixare):
| # | Sev | File:riga | Problema |
|---|---|---|---|
| A3-1 | **CRIT** | jobRunner.ts:1064-1084,1183-1206 | `rotateSessionWithLoginCheck()` può ritornare `null`; caller userebbe `session.page` → NPE + browser leak + profile lock. **Da verificare.** |
| A3-2 | **CRIT** | jobRunner.ts:1396-1456 | Handoff: exception dopo consumo sessione → `finally` non rientra → browser orfano; `closeBrowser().catch(()=>undefined)` inghiotte errori. **Da verificare.** |
| A3-3 | **CRIT** | orchestrator.ts:313-325 | `cleanupEnqueuedOnBlock` cancella solo job QUEUED, non i già-LOCKED→RUNNING → outreach stale rieseguito ore dopo un block. **Da verificare.** |
| A3-4 | HIGH | jobs.ts:61-116 | Nessun stale-lock timeout: job RUNNING di sessione crashata resta locked per sempre (no `locked_at < N min` reset). **Da verificare.** |
| A3-5 | HIGH | orchestrator.ts:701-722 | Pause con `baseMinutes=null` senza max-cap → account potenzialmente bloccato forever. **Da verificare.** |
| A3-6 | MED | scheduler.ts:1056-1058 | Enrichment count read-modify-write non atomico → 2 sessioni violano il cap 200/day. **Da verificare.** |
| A3-7 | IGI | jobRunner.ts(>1450) | Split in session-rotation / challenge / wind-down / health. |

---

## A4 — Secondo cervello / AI decision engine

**Mappa**: `aiDecisionEngine.ts(344)` (5 decision point + timeout 8s + fallback PROCEED) · `providerRegistry.ts(280)`
(routing per-purpose + guard zero-PII + circuit breaker) · `guardian.ts(294)` (fail-closed critical pause ≥30min) ·
`decisionFeedback.ts(215)` · `aiAdvisor.ts(163)`.

**Funziona**: decisioni→azioni reali (inviteWorker.ts:438-454 SKIP/DEFER/NOTIFY_HUMAN); zero-PII pseudonimizzazione
+ test sentinella (aiDecisionEngine.ts:230-234, vitest 100-203); guard PII-sensitive mai-cloud (providerRegistry.ts:179-191);
**nessun residuo "fable"**, routing `claude-opus-4-8` brain + `claude-haiku-4-5` light (domains.ts:269/272); timeout hard
con clearTimeout in finally (aiDecisionEngine.ts:86,114-123).

**Problemi**:
| # | Sev | File:riga | Problema |
|---|---|---|---|
| A4-1 | HIGH | aiDecisionEngine.ts:187-222 | `getAccuracyContext()` carica stats 14gg ma **non le inietta MAI** nel prompt → auto-calibrazione AI design-only, non operativa. |
| A4-2 | HIGH | decisionFeedback.ts:86-109 + aiDecisionEngine.ts:141 | `recordDecision` fire-and-forget: DB fail → feedback loop muore silenzioso. |
| A4-3 | MED | leadStateService.ts:104-107 | outcomeMap manca `REVIEW_REQUIRED` (da NOTIFY_HUMAN) → feedback incompleto su quel branch. |
| A4-4 | MED | guardian.ts:157-159 | Severity inattesa dall'AI → default soft a 'watch' senza warn/alert. |
| A4-5 | MED | inboxWorker.ts:294-304 | NOTIFY_HUMAN trattato come SKIP → reply bloccata senza escalation utente. |

---

## A5 — Collegamento al DB (SQLite locale + Supabase cloud)

**Mappa**: outbox `repositories/system.ts:92-237` (push + claim lease-lock) · fan-out `outboxDeliveries.ts` ·
sync worker `sync/supabaseSyncWorker.ts` · bridge `cloud/cloudBridge.ts:34` · client `cloud/supabaseDataClient.ts` ·
migration `db/migrations/*` + cloud `sync/migrations/*` + schema `sync/supabase.full.schema.sql`.

**Funziona**: outbox atomico `INSERT OR IGNORE` + idempotency_key UNIQUE (system.ts:102-109); claim lease-lock con
timeout crash-safe (system.ts:204-216); RPC idempotente cloud daily_stat (cloud_001:47-64); batch upsert chunk 200
(supabaseDataClient.ts:127-149); allowlist colonne anti-SQL-injection (stats.ts:24-46).

**Problemi**:
| # | Sev | File:riga | Problema |
|---|---|---|---|
| A5-1 | **CRIT** | system.ts:137-150 | GDPR erasure: se `supabaseSyncEnabled=false` → early return, evento erase NON arriva mai a cloud → non-compliant. |
| A5-2 | HIGH | supabase.full.schema.sql:8-12 | Schema drift NOTO (T1 gdpr 2026-06-12): colonne/tabelle cloud non nel file → schema canonico stale. |
| A5-3 | HIGH | 055_daily_stats_account_id.sql vs schema cloud | `daily_stats` locale 11 colonne; `daily_stats_cloud` manca follow_ups_sent/profile_views/likes_given/follows_given → perdita dati su sync. |
| A5-4 | MED | cloudBridge.ts:35-48 | Fallback outbox con `.catch()` vuoto → fallimento push non loggato. |
| A5-5 | MED | supabaseSyncWorker.ts:40-46 | Client Supabase senza timeout esplicito → blocco indefinito su rete lenta. |
| A5-6 | MED | system.ts:258-279 | Claim-lease scaduto non ripulisce `processing_owner` → ghost claim possibile. |

> NB: A5 ha sovrapposizioni con A2-1 (outbox loss) — convergono su "fallback outbox/cloud senza propagazione".

---

## Backlog consolidato (ordine: CRIT > HIGH > MED > IGI)

**CRIT** (verificare alla fonte poi fixare inline):
1. ✅ A1-1 askConfirmation default — **FATTO**.
2. A5-1 GDPR erasure con sink cloud disabilitato (compliance).
3. A2-1/A5-4 Outbox push loss silenzioso (alert + logging).
4. A3-1 NPE post session-rotate · A3-2 browser leak handoff · A3-3 job stale post-block.

**HIGH**: A2-2 enrichment retry · A2-3 checkpoint atomicity · A3-4 stale-lock timeout · A3-5 pause max-cap ·
A4-1 inject accuracy context · A4-2 recordDecision resiliente · A5-2/A5-3 schema drift + colonne divergenti.

**MED**: A1-2 cap askNumber · A2-4 selector fallback parity · A2-5 cloud metrics per-lead · A3-6 enrichment count atomico ·
A4-3/A4-4/A4-5 feedback/guardian/inbox · A5-5 timeout client · A5-6 ghost claim.

**IGI** (A13): split file >300 — bulkSaveOrchestrator(1839), salesNavigatorSync(1198), jobRunner(>1450),
adminCommands(1170), loopCommand(1167), utilCommands(875), index.ts(752), salesNavCommands(726).

## Prossimi passi
A6 (risk/antiban guards — circuit breaker aperti apollo/ddg/openai/telegram) → A7 scheduler → A8 proxy/stealth →
A9 n8n → A10 dashboard → A11 telemetria → A12 multi-account → A13 igiene completa. Poi triage CRIT verificati → fix inline.
